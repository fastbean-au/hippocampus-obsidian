{
  "swagger": "2.0",
  "info": {
    "title": "hippocampus.proto",
    "version": "version not set"
  },
  "tags": [
    {
      "name": "Hippocampus",
      "description": "HTTP annotations below map every RPC onto a REST-ish JSON endpoint under /v1, served by the\ngrpc-gateway alongside the native gRPC API. Path-bound fields (e.g. {id}) are taken from the\nURL; for GET/DELETE the remaining fields are taken from the query string, and for POST/PATCH\nthey are taken from the JSON body."
    }
  ],
  "consumes": [
    "application/json"
  ],
  "produces": [
    "application/json"
  ],
  "paths": {
    "/v1/callbacks/queue": {
      "get": {
        "summary": "GetCallbackQueue reports what the outbound callback queue is holding: how many deliveries are\nwaiting, how old the oldest is, and a page of the pending ones.",
        "description": "Where the forgotten log answers \"what went\", the callbacks are the push half of the same\nquestion - and a queue is the thing that stands between a deletion and whoever was told about\nit. This exists so an operator can see a backlog before deciding what to do about it: a purge\nwith no way to see what is being purged is not an operator tool.\n\nIt never returns a delivery's payload. A queued delivery may carry memory bodies\n(callbacks.includeBodies), and this is a view of a backlog rather than a second way to read\nthe store's contents.",
        "operationId": "Hippocampus_GetCallbackQueue",
        "responses": {
          "200": {
            "description": "A successful response.",
            "schema": {
              "$ref": "#/definitions/v1GetCallbackQueueResponse"
            }
          },
          "default": {
            "description": "An unexpected error response.",
            "schema": {
              "$ref": "#/definitions/rpcStatus"
            }
          }
        },
        "parameters": [
          {
            "name": "kind",
            "description": "UNSPECIFIED returns every kind",
            "in": "query",
            "required": false,
            "type": "string",
            "enum": [
              "CALLBACK_KIND_UNSPECIFIED",
              "CALLBACK_KIND_MEMORY_FORGOTTEN",
              "CALLBACK_KIND_EVENT_FORGOTTEN",
              "CALLBACK_KIND_SLEEP_COMPLETED",
              "CALLBACK_KIND_MEMORIES_AT_RISK"
            ],
            "default": "CALLBACK_KIND_UNSPECIFIED"
          },
          {
            "name": "afterSeq",
            "description": "pagination: return deliveries above this seq (pass the highest seq of the previous page)",
            "in": "query",
            "required": false,
            "type": "string",
            "format": "int64"
          },
          {
            "name": "limit",
            "description": "\u003c= 0 selects the default (100), values above the cap (1000) are clamped down to it",
            "in": "query",
            "required": false,
            "type": "integer",
            "format": "int32"
          }
        ],
        "tags": [
          "Hippocampus"
        ]
      }
    },
    "/v1/callbacks/queue/delete": {
      "post": {
        "summary": "DeleteCallbackQueue empties the outbound callback queue, or the part of it queued before a\ncutoff.",
        "description": "It exists for the reason DeleteForgottenMemories does: the queue's automatic bounds\n(callbacks.maxRows/maxAgeHours) only apply while callbacks are enabled, so turning the feature\noff leaves whatever was already queued in place rather than destroying it. Discarding those\ndeliveries is therefore always an explicit request - this one - and it is a real discard: a\ndeleted delivery is a notification nobody will ever receive.",
        "operationId": "Hippocampus_DeleteCallbackQueue",
        "responses": {
          "200": {
            "description": "A successful response.",
            "schema": {
              "$ref": "#/definitions/v1DeleteCallbackQueueResponse"
            }
          },
          "default": {
            "description": "An unexpected error response.",
            "schema": {
              "$ref": "#/definitions/rpcStatus"
            }
          }
        },
        "parameters": [
          {
            "name": "body",
            "description": "DeleteCallbackQueueRequest empties the callback queue. Supply before_time to discard only what was\nqueued before it, or all to empty the queue entirely - one of the two is required, so an empty\nrequest can never silently discard every pending notification.",
            "in": "body",
            "required": true,
            "schema": {
              "$ref": "#/definitions/v1DeleteCallbackQueueRequest"
            }
          }
        ],
        "tags": [
          "Hippocampus"
        ]
      }
    },
    "/v1/clear": {
      "post": {
        "summary": "Clear deletes exactly the records captured by the Export/Transfer run that produced the\nmanifest. Memories recalled (or re-created) since the capture survive, as do events that\nstill have memories.",
        "operationId": "Hippocampus_Clear",
        "responses": {
          "200": {
            "description": "A successful response.",
            "schema": {
              "$ref": "#/definitions/v1ClearResponse"
            }
          },
          "default": {
            "description": "An unexpected error response.",
            "schema": {
              "$ref": "#/definitions/rpcStatus"
            }
          }
        },
        "parameters": [
          {
            "name": "body",
            "description": "Clear deletes exactly the records captured by the Export/Transfer run that produced the\nmanifest: memories are deleted only if neither created nor recalled since the snapshot, and\nevents only once they have no memories left. Manifests are held in memory - a restart discards\nthem.",
            "in": "body",
            "required": true,
            "schema": {
              "$ref": "#/definitions/v1ClearRequest"
            }
          }
        ],
        "tags": [
          "Hippocampus"
        ]
      }
    },
    "/v1/consolidation/explain": {
      "post": {
        "summary": "ExplainConsolidation reports where individual memories stand against the consolidation rules -\neach one's computed value, the threshold it is measured against, and how long it has before it\ncrosses - together with the capacity pressure scaling that threshold and, optionally, the decay\ncurve the configured algorithm produces. It reads nothing but the memories named in the\nrequest and deletes nothing.",
        "description": "Where PreviewConsolidation answers \"what would a cycle forget now\" across the whole store,\nthis answers \"where does this memory stand, and when is it due\" for memories the caller\nalready has in hand - which is why it needs only the reader tier: it enumerates nothing.\nRejected with FailedPrecondition on a read/write replica (consolidation.enabled: false),\nwhose configuration is not the one its store is consolidated under.",
        "operationId": "Hippocampus_ExplainConsolidation",
        "responses": {
          "200": {
            "description": "A successful response.",
            "schema": {
              "$ref": "#/definitions/v1ExplainConsolidationResponse"
            }
          },
          "default": {
            "description": "An unexpected error response.",
            "schema": {
              "$ref": "#/definitions/rpcStatus"
            }
          }
        },
        "parameters": [
          {
            "name": "body",
            "description": "ExplainConsolidationRequest asks where the named memories stand against the consolidation rules,\nand optionally for the decay curve of the current configuration. Both parts are optional: ids\nwithout a curve values just those memories, a curve without ids reports the configuration and the\ncurrent capacity pressure alone. Ids that do not exist are simply absent from the response.",
            "in": "body",
            "required": true,
            "schema": {
              "$ref": "#/definitions/v1ExplainConsolidationRequest"
            }
          }
        ],
        "tags": [
          "Hippocampus"
        ]
      }
    },
    "/v1/consolidation/status": {
      "get": {
        "summary": "GetConsolidationStatus reports what this instance's sleep cycle is doing: when the next timed\ncycle is due, whether one is running, and what the last one actually did.",
        "description": "It completes the forgetting story the other three RPCs tell. PreviewConsolidation says what\nwould go, ExplainConsolidation where a given memory stands, GetForgottenMemories what went -\nand none of the three says WHEN. Without that, a store that forgets on a schedule is\nindistinguishable from a store that is doing nothing, which is exactly how the console read\nbefore this existed.\n\nReader tier: it names no stored record and enumerates nothing. The aggregates it reports are\nthe same store-global figures ExplainConsolidation already serves a reader, and for the same\nreason - they are what decides that caller's own memories' fate.\n\nUnlike Sleep, PreviewConsolidation and ExplainConsolidation, it does NOT fail on a read/write\nreplica: it answers with consolidation_enabled false and everything else zero. Refusing would\nbe self-defeating, since telling a client it is talking to a replica is the RPC's whole job.",
        "operationId": "Hippocampus_GetConsolidationStatus",
        "responses": {
          "200": {
            "description": "A successful response.",
            "schema": {
              "$ref": "#/definitions/v1GetConsolidationStatusResponse"
            }
          },
          "default": {
            "description": "An unexpected error response.",
            "schema": {
              "$ref": "#/definitions/rpcStatus"
            }
          }
        },
        "tags": [
          "Hippocampus"
        ]
      }
    },
    "/v1/events": {
      "get": {
        "summary": "GetEvents lists events filtered by time range, significance range, group, metadata, name\nsubstring, open/ended state and link neighbourhood, paginated by limit/offset (see\nGetEventsRequest). significance_extremum swaps the significance_min/max range for \"only events\ntied at the highest (or lowest) significance value\" instead. order_by/order_dir choose the\nsort field and direction.",
        "operationId": "Hippocampus_GetEvents",
        "responses": {
          "200": {
            "description": "A successful response.",
            "schema": {
              "$ref": "#/definitions/v1GetEventsResponse"
            }
          },
          "default": {
            "description": "An unexpected error response.",
            "schema": {
              "$ref": "#/definitions/rpcStatus"
            }
          }
        },
        "parameters": [
          {
            "name": "timeStartMin",
            "description": "UnixNano inclusive lower bound on time_start; 0 (the default) means no lower bound",
            "in": "query",
            "required": false,
            "type": "string",
            "format": "int64"
          },
          {
            "name": "timeStartMax",
            "description": "UnixNano inclusive upper bound on time_start; 0 (the default) means no upper bound",
            "in": "query",
            "required": false,
            "type": "string",
            "format": "int64"
          },
          {
            "name": "timeEndMin",
            "description": "UnixNano inclusive lower bound on time_end; 0 (the default) means no lower bound",
            "in": "query",
            "required": false,
            "type": "string",
            "format": "int64"
          },
          {
            "name": "timeEndMax",
            "description": "UnixNano inclusive upper bound on time_end; 0 (the default) means no upper bound",
            "in": "query",
            "required": false,
            "type": "string",
            "format": "int64"
          },
          {
            "name": "significanceMin",
            "description": "inclusive lower bound on significance; 0 (the default) means no bound, NOT a literal significance-of-zero filter",
            "in": "query",
            "required": false,
            "type": "integer",
            "format": "int32"
          },
          {
            "name": "significanceMax",
            "description": "inclusive upper bound on significance; 0 (the default) means no bound, NOT a literal significance-of-zero filter",
            "in": "query",
            "required": false,
            "type": "integer",
            "format": "int32"
          },
          {
            "name": "memories",
            "description": "when true, include each matching event's memories in the response",
            "in": "query",
            "required": false,
            "type": "boolean"
          },
          {
            "name": "group",
            "description": "optional: restrict to events carrying this group label",
            "in": "query",
            "required": false,
            "type": "string"
          },
          {
            "name": "orderBy",
            "description": "the field to sort on; defaults to \"timestamp\" (most recently started first). One of: \"significance\" (naturally desc, then time_start, then id), \"timestamp\" (time_start, naturally desc), \"time_end\" (naturally desc; an event that has not ended stores 0, so those sort as the OLDEST rather than the most recent - use time_end_min to exclude them), \"name\" (naturally asc), \"group\" (naturally asc, then time_start), \"link_significance\" (naturally desc, then significance), \"id\" (naturally asc). order_dir overrides the natural direction. Every ordering carries an id tiebreaker, so offset pagination is stable",
            "in": "query",
            "required": false,
            "type": "string"
          },
          {
            "name": "limit",
            "description": "page size; a value \u003c= 0 selects the server default (25); values above the server cap (200) are silently clamped down to it",
            "in": "query",
            "required": false,
            "type": "integer",
            "format": "int32"
          },
          {
            "name": "offset",
            "description": "rows to skip for pagination; negative values are clamped to 0; no upper bound",
            "in": "query",
            "required": false,
            "type": "integer",
            "format": "int32"
          },
          {
            "name": "significanceExtremum",
            "description": "when set, ignore significance_min/significance_max and return only events tied at that significance value among the other filters (time range, group, metadata) - not a range; 0 (default) applies no extremum\n\n - SIGNIFICANCE_EXTREMUM_UNSPECIFIED: no extremum filtering; significance_min/significance_max (if set) apply as a normal range\n - SIGNIFICANCE_EXTREMUM_HIGHEST: return only events at the highest significance value among those matching the other filters\n - SIGNIFICANCE_EXTREMUM_LOWEST: return only events at the lowest significance value among those matching the other filters",
            "in": "query",
            "required": false,
            "type": "string",
            "enum": [
              "SIGNIFICANCE_EXTREMUM_UNSPECIFIED",
              "SIGNIFICANCE_EXTREMUM_HIGHEST",
              "SIGNIFICANCE_EXTREMUM_LOWEST"
            ],
            "default": "SIGNIFICANCE_EXTREMUM_UNSPECIFIED"
          },
          {
            "name": "metadata",
            "description": "optional: restrict to events whose metadata carries every one of these \"key=value\" pairs (AND, exact match), exactly as GetMemoriesRequest.metadata",
            "in": "query",
            "required": false,
            "type": "array",
            "items": {
              "type": "string"
            },
            "collectionFormat": "multi"
          },
          {
            "name": "memoryCounts",
            "description": "when true, populate each returned event's memory_count. Separate from `memories` and opt-in because it is a second query per page: it reports how many memories an event holds without transferring any of them, which is what a listing wants and what `memories` is far too expensive for",
            "in": "query",
            "required": false,
            "type": "boolean"
          },
          {
            "name": "orderDir",
            "description": "optional: reverse order_by's sort direction; UNSPECIFIED (the default) uses that field's natural direction. Applies to the whole ordering, tiebreakers included, so ASC returns exactly the reverse of DESC\n\n - SORT_DIRECTION_UNSPECIFIED: sort in the order_by field's natural direction (see above)\n - SORT_DIRECTION_ASC: ascending, whatever the field's natural direction\n - SORT_DIRECTION_DESC: descending, whatever the field's natural direction",
            "in": "query",
            "required": false,
            "type": "string",
            "enum": [
              "SORT_DIRECTION_UNSPECIFIED",
              "SORT_DIRECTION_ASC",
              "SORT_DIRECTION_DESC"
            ],
            "default": "SORT_DIRECTION_UNSPECIFIED"
          },
          {
            "name": "ended",
            "description": "optional: FALSE returns only events that have not ended (time_end = 0), TRUE only those that have; UNSPECIFIED (default) applies no restriction. This is what answers \"what is still running?\" - time_end_min cannot, because 0 there means no bound. The events' counterpart to GetMemoriesRequest.recalled, and there for the same reason\n\n - UNSPECIFIED: not set; on Memory.is_binary the server treats this the same as FALSE, on a filter it applies no restriction\n - FALSE: explicitly false\n - TRUE: explicitly true",
            "in": "query",
            "required": false,
            "type": "string",
            "enum": [
              "UNSPECIFIED",
              "FALSE",
              "TRUE"
            ],
            "default": "UNSPECIFIED"
          },
          {
            "name": "nameContains",
            "description": "optional: restrict to events whose name contains this substring, case-insensitively. Unindexed, exactly as the group and metadata filters are, and a substring match rather than a content search - an event's name and description are in no search index. Empty (the default) applies no restriction",
            "in": "query",
            "required": false,
            "type": "string"
          },
          {
            "name": "links",
            "description": "when true, populate each returned event's links (outbound only, like Event.links - ask GetEventLinks for both directions)",
            "in": "query",
            "required": false,
            "type": "boolean"
          },
          {
            "name": "linkedTo",
            "description": "optional: restrict to events linked to this event id, in either direction (one hop). NotFound if that event does not exist. The events' counterpart to GetMemoriesRequest.linked_to",
            "in": "query",
            "required": false,
            "type": "string"
          }
        ],
        "tags": [
          "Hippocampus"
        ]
      },
      "post": {
        "summary": "Events\nStoreEvent creates an event, optionally with nested memories (each defaulted to the new\nevent's id when unset). An event below event.minimumSignificance is quietly dropped - see\nStoreEventResponse.",
        "operationId": "Hippocampus_StoreEvent",
        "responses": {
          "200": {
            "description": "A successful response.",
            "schema": {
              "$ref": "#/definitions/v1StoreEventResponse"
            }
          },
          "default": {
            "description": "An unexpected error response.",
            "schema": {
              "$ref": "#/definitions/rpcStatus"
            }
          }
        },
        "parameters": [
          {
            "name": "body",
            "description": "Event is a named time span with its own significance, optionally carrying links to\nother events and/or nested memories. memories/links are populated on read only when\nrequested (GetEventById, GetEvents); on StoreEvent nested memories are write-only input, each\nstored via the same rules as StoreMemory.",
            "in": "body",
            "required": true,
            "schema": {
              "$ref": "#/definitions/v1Event"
            }
          }
        ],
        "tags": [
          "Hippocampus"
        ]
      }
    },
    "/v1/events/delete-by-filter": {
      "post": {
        "summary": "DeleteEventsByFilter deletes every event matching the filter - the same selection the\nGetEvents listing takes - rather than one event by id, and is the events' half of what\noffboarding a group needs. Like its memory counterpart it refuses an empty filter, and\nGetEvents with the same fields is the dry run.",
        "description": "delete_memories decides what happens to each event's memories, exactly as DeleteEvent's\nmemories flag does: set, they go with their event; unset, they survive with no event.",
        "operationId": "Hippocampus_DeleteEventsByFilter",
        "responses": {
          "200": {
            "description": "A successful response.",
            "schema": {
              "$ref": "#/definitions/v1DeleteEventsByFilterResponse"
            }
          },
          "default": {
            "description": "An unexpected error response.",
            "schema": {
              "$ref": "#/definitions/rpcStatus"
            }
          }
        },
        "parameters": [
          {
            "name": "body",
            "description": "DeleteEventsByFilterRequest selects the events to delete. Every field means what the same field\nmeans on GetEventsRequest; order_by/order_dir/limit/offset, memories, memory_counts, links and\nlinked_to are absent for the reasons DeleteMemoriesByFilterRequest gives.\n\nAt least one selecting field must be set, delete_memories excepted - it says what to do with\nwhat is selected, not what to select, so a request carrying only delete_memories is still\nrefused.",
            "in": "body",
            "required": true,
            "schema": {
              "$ref": "#/definitions/v1DeleteEventsByFilterRequest"
            }
          }
        ],
        "tags": [
          "Hippocampus"
        ]
      }
    },
    "/v1/events/merge": {
      "post": {
        "summary": "MergeEvents re-points every memory of merge_from onto merge_to. merge_to must already exist;\nmerge_from need not - an absent one simply matches no memories.",
        "operationId": "Hippocampus_MergeEvents",
        "responses": {
          "200": {
            "description": "A successful response.",
            "schema": {
              "$ref": "#/definitions/v1GeneralResponse"
            }
          },
          "default": {
            "description": "An unexpected error response.",
            "schema": {
              "$ref": "#/definitions/rpcStatus"
            }
          }
        },
        "parameters": [
          {
            "name": "body",
            "in": "body",
            "required": true,
            "schema": {
              "$ref": "#/definitions/v1MergeEventsRequest"
            }
          }
        ],
        "tags": [
          "Hippocampus"
        ]
      }
    },
    "/v1/events/{eventId}/summarise": {
      "post": {
        "summary": "SummariseMemories condenses an event's memories into a single summary using the optional\nembedded LLM (llm.enabled), then replaces them with it exactly as ReplaceMemoriesWithSummary\ndoes - so the service generates the summary the caller would otherwise have to supply. Fails\nwith FAILED_PRECONDITION when no summariser is configured. See SummariseMemoriesRequest.",
        "operationId": "Hippocampus_SummariseMemories",
        "responses": {
          "200": {
            "description": "A successful response.",
            "schema": {
              "$ref": "#/definitions/v1SummariseMemoriesResponse"
            }
          },
          "default": {
            "description": "An unexpected error response.",
            "schema": {
              "$ref": "#/definitions/rpcStatus"
            }
          }
        },
        "parameters": [
          {
            "name": "eventId",
            "in": "path",
            "required": true,
            "type": "string"
          },
          {
            "name": "body",
            "in": "body",
            "required": true,
            "schema": {
              "$ref": "#/definitions/HippocampusSummariseMemoriesBody"
            }
          }
        ],
        "tags": [
          "Hippocampus"
        ]
      }
    },
    "/v1/events/{eventId}/summary": {
      "post": {
        "summary": "Summarisation\nReplaceMemoriesWithSummary deletes every memory of an event and replaces them with a single\ncaller-supplied summary memory, in one transaction; see ReplaceMemoriesWithSummaryRequest.",
        "operationId": "Hippocampus_ReplaceMemoriesWithSummary",
        "responses": {
          "200": {
            "description": "A successful response.",
            "schema": {
              "$ref": "#/definitions/v1ReplaceMemoriesWithSummaryResponse"
            }
          },
          "default": {
            "description": "An unexpected error response.",
            "schema": {
              "$ref": "#/definitions/rpcStatus"
            }
          }
        },
        "parameters": [
          {
            "name": "eventId",
            "in": "path",
            "required": true,
            "type": "string"
          },
          {
            "name": "summary",
            "in": "body",
            "required": true,
            "schema": {
              "$ref": "#/definitions/v1Memory"
            }
          }
        ],
        "tags": [
          "Hippocampus"
        ]
      }
    },
    "/v1/events/{id}": {
      "get": {
        "summary": "GetEventById fetches a single event by id; set memories to also load its nested memories.\nNotFound if the event does not exist.",
        "operationId": "Hippocampus_GetEventById",
        "responses": {
          "200": {
            "description": "A successful response.",
            "schema": {
              "$ref": "#/definitions/v1GetEventResponse"
            }
          },
          "default": {
            "description": "An unexpected error response.",
            "schema": {
              "$ref": "#/definitions/rpcStatus"
            }
          }
        },
        "parameters": [
          {
            "name": "id",
            "description": "the event to fetch",
            "in": "path",
            "required": true,
            "type": "string"
          },
          {
            "name": "memories",
            "description": "true also loads the event's memories into the response",
            "in": "query",
            "required": false,
            "type": "boolean"
          },
          {
            "name": "memoryCounts",
            "description": "true populates memory_count, exactly as GetEventsRequest.memory_counts does (and for the same reason: it reports how many memories the event holds without transferring any of them)",
            "in": "query",
            "required": false,
            "type": "boolean"
          },
          {
            "name": "links",
            "description": "true also loads the event's outbound links into the response (see Event.links); GetEventLinks reads both directions",
            "in": "query",
            "required": false,
            "type": "boolean"
          }
        ],
        "tags": [
          "Hippocampus"
        ]
      },
      "delete": {
        "summary": "DeleteEvent deletes an event. When memories is true its memories are deleted too; otherwise\nthey are kept but detached (their event_id is cleared). NotFound if the event does not exist.",
        "operationId": "Hippocampus_DeleteEvent",
        "responses": {
          "200": {
            "description": "A successful response.",
            "schema": {
              "$ref": "#/definitions/v1GeneralResponse"
            }
          },
          "default": {
            "description": "An unexpected error response.",
            "schema": {
              "$ref": "#/definitions/rpcStatus"
            }
          }
        },
        "parameters": [
          {
            "name": "id",
            "in": "path",
            "required": true,
            "type": "string"
          },
          {
            "name": "memories",
            "description": "true also deletes the event's memories; false only detaches them",
            "in": "query",
            "required": false,
            "type": "boolean"
          }
        ],
        "tags": [
          "Hippocampus"
        ]
      },
      "patch": {
        "summary": "UpdateEvent applies a partial update to an existing event: only the fields carrying a value\n(time_start, time_end, significance, name, description, group, metadata) overwrite the stored\nrow, with clear_group/clear_metadata to unset the two whose own zero value cannot say it.\nmemories and links are NOT updatable here - nested memories are a StoreEvent input, and links\nare edited through LinkEvents/UnlinkEvents, which can report what became of each one and\nreject a target that does not exist. An unknown id returns NotFound (no event is created).",
        "description": "EndEvent and UpdateEventSignificance remain the convenience routes for the two fields they\nname - EndEvent defaults time_end to now, UpdateEventSignificance carries a placement - and\nboth fields are also settable here, placement included, so editing several at once is one call\nrather than three.",
        "operationId": "Hippocampus_UpdateEvent",
        "responses": {
          "200": {
            "description": "A successful response.",
            "schema": {
              "$ref": "#/definitions/v1GeneralResponse"
            }
          },
          "default": {
            "description": "An unexpected error response.",
            "schema": {
              "$ref": "#/definitions/rpcStatus"
            }
          }
        },
        "parameters": [
          {
            "name": "id",
            "description": "auto-generated UUID if omitted on create; max 128 characters",
            "in": "path",
            "required": true,
            "type": "string"
          },
          {
            "name": "body",
            "in": "body",
            "required": true,
            "schema": {
              "$ref": "#/definitions/HippocampusUpdateEventBody"
            }
          }
        ],
        "tags": [
          "Hippocampus"
        ]
      }
    },
    "/v1/events/{id}/end": {
      "post": {
        "summary": "EndEvent sets an event's time_end (defaults to now when unset). NotFound if the event does\nnot exist.",
        "operationId": "Hippocampus_EndEvent",
        "responses": {
          "200": {
            "description": "A successful response.",
            "schema": {
              "$ref": "#/definitions/v1GeneralResponse"
            }
          },
          "default": {
            "description": "An unexpected error response.",
            "schema": {
              "$ref": "#/definitions/rpcStatus"
            }
          }
        },
        "parameters": [
          {
            "name": "id",
            "description": "the event to end",
            "in": "path",
            "required": true,
            "type": "string"
          },
          {
            "name": "body",
            "in": "body",
            "required": true,
            "schema": {
              "$ref": "#/definitions/HippocampusEndEventBody"
            }
          }
        ],
        "tags": [
          "Hippocampus"
        ]
      }
    },
    "/v1/events/{id}/links": {
      "get": {
        "summary": "GetEventLinks is GetMemoryLinks for events; the same rules apply.",
        "operationId": "Hippocampus_GetEventLinks",
        "responses": {
          "200": {
            "description": "A successful response.",
            "schema": {
              "$ref": "#/definitions/v1GetLinksResponse"
            }
          },
          "default": {
            "description": "An unexpected error response.",
            "schema": {
              "$ref": "#/definitions/rpcStatus"
            }
          }
        },
        "parameters": [
          {
            "name": "id",
            "description": "the event whose links to return; must exist",
            "in": "path",
            "required": true,
            "type": "string"
          },
          {
            "name": "direction",
            "description": "which directions to return; unset means BOTH\n\n - LINK_DIRECTION_UNSPECIFIED: treated as BOTH\n - LINK_DIRECTION_BOTH: links in either direction\n - LINK_DIRECTION_OUTBOUND: only links this item declared, pointing away from it\n - LINK_DIRECTION_INBOUND: only links other items declared, pointing at it",
            "in": "query",
            "required": false,
            "type": "string",
            "enum": [
              "LINK_DIRECTION_UNSPECIFIED",
              "LINK_DIRECTION_BOTH",
              "LINK_DIRECTION_OUTBOUND",
              "LINK_DIRECTION_INBOUND"
            ],
            "default": "LINK_DIRECTION_UNSPECIFIED"
          }
        ],
        "tags": [
          "Hippocampus"
        ]
      },
      "post": {
        "summary": "LinkEvents is LinkMemories for events; the same rules apply.",
        "operationId": "Hippocampus_LinkEvents",
        "responses": {
          "200": {
            "description": "A successful response.",
            "schema": {
              "$ref": "#/definitions/v1GeneralResponse"
            }
          },
          "default": {
            "description": "An unexpected error response.",
            "schema": {
              "$ref": "#/definitions/rpcStatus"
            }
          }
        },
        "parameters": [
          {
            "name": "id",
            "description": "the event the links start from; must exist",
            "in": "path",
            "required": true,
            "type": "string"
          },
          {
            "name": "body",
            "in": "body",
            "required": true,
            "schema": {
              "$ref": "#/definitions/HippocampusLinkEventsBody"
            }
          }
        ],
        "tags": [
          "Hippocampus"
        ]
      }
    },
    "/v1/events/{id}/links/delete": {
      "post": {
        "summary": "UnlinkEvents is UnlinkMemories for events; the same rules apply.",
        "operationId": "Hippocampus_UnlinkEvents",
        "responses": {
          "200": {
            "description": "A successful response.",
            "schema": {
              "$ref": "#/definitions/v1GeneralResponse"
            }
          },
          "default": {
            "description": "An unexpected error response.",
            "schema": {
              "$ref": "#/definitions/rpcStatus"
            }
          }
        },
        "parameters": [
          {
            "name": "id",
            "description": "the event the links start from; must exist",
            "in": "path",
            "required": true,
            "type": "string"
          },
          {
            "name": "body",
            "in": "body",
            "required": true,
            "schema": {
              "$ref": "#/definitions/HippocampusUnlinkEventsBody"
            }
          }
        ],
        "tags": [
          "Hippocampus"
        ]
      }
    },
    "/v1/events/{id}/significance": {
      "patch": {
        "summary": "UpdateEventSignificance changes an event's significance, either as an absolute value or via\nplacement (see SignificancePlacement). NotFound if the event does not exist.",
        "operationId": "Hippocampus_UpdateEventSignificance",
        "responses": {
          "200": {
            "description": "A successful response.",
            "schema": {
              "$ref": "#/definitions/v1GeneralResponse"
            }
          },
          "default": {
            "description": "An unexpected error response.",
            "schema": {
              "$ref": "#/definitions/rpcStatus"
            }
          }
        },
        "parameters": [
          {
            "name": "id",
            "description": "the event to update",
            "in": "path",
            "required": true,
            "type": "string"
          },
          {
            "name": "body",
            "in": "body",
            "required": true,
            "schema": {
              "$ref": "#/definitions/HippocampusUpdateEventSignificanceBody"
            }
          }
        ],
        "tags": [
          "Hippocampus"
        ]
      }
    },
    "/v1/export": {
      "post": {
        "summary": "Transfer and archive (embedded -\u003e centralised)\nExport snapshots the whole store into an archive object in S3 and records a manifest of\nexactly what was captured; with clear set, the captured records are deleted once the upload\nhas succeeded.",
        "operationId": "Hippocampus_Export",
        "responses": {
          "200": {
            "description": "A successful response.",
            "schema": {
              "$ref": "#/definitions/v1ExportResponse"
            }
          },
          "default": {
            "description": "An unexpected error response.",
            "schema": {
              "$ref": "#/definitions/rpcStatus"
            }
          }
        },
        "parameters": [
          {
            "name": "body",
            "description": "Export snapshots every event and memory into an archive object in S3 and records a manifest of\nexactly what was captured; with clear set, the captured records are deleted after a successful\nupload (writes and recalls landing mid-run survive, exactly as Clear does).",
            "in": "body",
            "required": true,
            "schema": {
              "$ref": "#/definitions/v1ExportRequest"
            }
          }
        ],
        "tags": [
          "Hippocampus"
        ]
      }
    },
    "/v1/import": {
      "post": {
        "summary": "Import streams an archive object from S3 into the store, upserting every record by id with\nits full state preserved. Idempotent; aborts on the first error.",
        "operationId": "Hippocampus_Import",
        "responses": {
          "200": {
            "description": "A successful response.",
            "schema": {
              "$ref": "#/definitions/v1ImportResponse"
            }
          },
          "default": {
            "description": "An unexpected error response.",
            "schema": {
              "$ref": "#/definitions/rpcStatus"
            }
          }
        },
        "parameters": [
          {
            "name": "body",
            "in": "body",
            "required": true,
            "schema": {
              "$ref": "#/definitions/v1ImportRequest"
            }
          }
        ],
        "tags": [
          "Hippocampus"
        ]
      }
    },
    "/v1/import/batch": {
      "post": {
        "summary": "ImportBatch upserts full-state rows by id directly - the ingest half of Transfer. Nothing is\ndefaulted or gated on minimum significance; the rows carry their history.",
        "operationId": "Hippocampus_ImportBatch",
        "responses": {
          "200": {
            "description": "A successful response.",
            "schema": {
              "$ref": "#/definitions/v1ImportBatchResponse"
            }
          },
          "default": {
            "description": "An unexpected error response.",
            "schema": {
              "$ref": "#/definitions/rpcStatus"
            }
          }
        },
        "parameters": [
          {
            "name": "body",
            "description": "ImportBatchRequest carries full-state rows (original timestamps, recall state, significance,\ngroup, event links) to be upserted by id into the store as-is - a data migration, not a fresh\nwrite. It is the ingest half of Transfer and is re-sendable: re-importing the same rows is\nidempotent.",
            "in": "body",
            "required": true,
            "schema": {
              "$ref": "#/definitions/v1ImportBatchRequest"
            }
          }
        ],
        "tags": [
          "Hippocampus"
        ]
      }
    },
    "/v1/memories": {
      "get": {
        "summary": "GetMemories lists memories filtered by time range, significance range, group, metadata, and\nrecall state (recalled/recall_count/time_recalled, plus is_summary and is_binary), paginated\nby limit/offset (see GetMemoriesRequest). significance_extremum swaps the significance_min/max\nrange for \"only memories tied at the highest (or lowest) significance value\" instead.\norder_by/order_dir choose the sort field and direction.",
        "operationId": "Hippocampus_GetMemories",
        "responses": {
          "200": {
            "description": "A successful response.",
            "schema": {
              "$ref": "#/definitions/v1GetMemoriesResponse"
            }
          },
          "default": {
            "description": "An unexpected error response.",
            "schema": {
              "$ref": "#/definitions/rpcStatus"
            }
          }
        },
        "parameters": [
          {
            "name": "timestampMin",
            "description": "UnixNano inclusive lower bound on time_stamp; 0 (the default) means no lower bound",
            "in": "query",
            "required": false,
            "type": "string",
            "format": "int64"
          },
          {
            "name": "timestampMax",
            "description": "UnixNano inclusive upper bound on time_stamp; 0 (the default) means no upper bound",
            "in": "query",
            "required": false,
            "type": "string",
            "format": "int64"
          },
          {
            "name": "significanceMin",
            "description": "inclusive lower bound on significance; 0 (the default) means no bound, NOT a literal significance-of-zero filter",
            "in": "query",
            "required": false,
            "type": "integer",
            "format": "int32"
          },
          {
            "name": "significanceMax",
            "description": "inclusive upper bound on significance; 0 (the default) means no bound, NOT a literal significance-of-zero filter",
            "in": "query",
            "required": false,
            "type": "integer",
            "format": "int32"
          },
          {
            "name": "group",
            "description": "optional: restrict to memories carrying this group label",
            "in": "query",
            "required": false,
            "type": "string"
          },
          {
            "name": "orderBy",
            "description": "the field to sort on; defaults to \"timestamp\" (most recently stored first). One of: \"significance\" (naturally desc, then time_stamp, then id), \"timestamp\" (time_stamp, naturally desc), \"time_recalled\" (naturally desc; a never-recalled memory stores 0, so those sort as the LEAST recently recalled rather than being excluded - use recalled to ask about them), \"recall_count\" (naturally desc, then time_stamp), \"group\" (naturally asc, then time_stamp), \"link_significance\" (naturally desc, then significance), \"id\" (naturally asc). order_dir overrides the natural direction. Every ordering carries an id tiebreaker, so offset pagination is stable",
            "in": "query",
            "required": false,
            "type": "string"
          },
          {
            "name": "limit",
            "description": "page size; a value \u003c= 0 selects the server default (25); values above the server cap (200) are silently clamped down to it",
            "in": "query",
            "required": false,
            "type": "integer",
            "format": "int32"
          },
          {
            "name": "offset",
            "description": "rows to skip for pagination; negative values are clamped to 0; no upper bound",
            "in": "query",
            "required": false,
            "type": "integer",
            "format": "int32"
          },
          {
            "name": "significanceExtremum",
            "description": "when set, ignore significance_min/significance_max and return only memories tied at that significance value among the other filters (time range, group, metadata, recall state) - not a range; 0 (default) applies no extremum\n\n - SIGNIFICANCE_EXTREMUM_UNSPECIFIED: no extremum filtering; significance_min/significance_max (if set) apply as a normal range\n - SIGNIFICANCE_EXTREMUM_HIGHEST: return only events at the highest significance value among those matching the other filters\n - SIGNIFICANCE_EXTREMUM_LOWEST: return only events at the lowest significance value among those matching the other filters",
            "in": "query",
            "required": false,
            "type": "string",
            "enum": [
              "SIGNIFICANCE_EXTREMUM_UNSPECIFIED",
              "SIGNIFICANCE_EXTREMUM_HIGHEST",
              "SIGNIFICANCE_EXTREMUM_LOWEST"
            ],
            "default": "SIGNIFICANCE_EXTREMUM_UNSPECIFIED"
          },
          {
            "name": "linkedTo",
            "description": "optional: restrict to memories linked to this memory id, in either direction (one hop). NotFound if that memory does not exist",
            "in": "query",
            "required": false,
            "type": "string"
          },
          {
            "name": "links",
            "description": "when true, populate each returned memory's links",
            "in": "query",
            "required": false,
            "type": "boolean"
          },
          {
            "name": "metadata",
            "description": "optional: restrict to memories whose metadata carries every one of these \"key=value\" pairs (AND, exact match). Split on the FIRST '=', so a value may itself contain '='; the key must match the metadata key charset. A repeated string rather than a map because a map cannot be bound from a URL query string - over HTTP this is ?metadata=source%3Dslack\u0026metadata=project%3Dx. Unindexed, exactly as the group filter is",
            "in": "query",
            "required": false,
            "type": "array",
            "items": {
              "type": "string"
            },
            "collectionFormat": "multi"
          },
          {
            "name": "recalled",
            "description": "optional: FALSE returns only never-recalled memories (recall_count = 0), TRUE only those recalled at least once; UNSPECIFIED (default) applies no restriction. This is what answers \"what have I never recalled?\" - recall_count_max cannot, because 0 there means no bound\n\n - UNSPECIFIED: not set; on Memory.is_binary the server treats this the same as FALSE, on a filter it applies no restriction\n - FALSE: explicitly false\n - TRUE: explicitly true",
            "in": "query",
            "required": false,
            "type": "string",
            "enum": [
              "UNSPECIFIED",
              "FALSE",
              "TRUE"
            ],
            "default": "UNSPECIFIED"
          },
          {
            "name": "recallCountMin",
            "description": "inclusive lower bound on recall_count; 0 (the default) means no bound, NOT a literal never-recalled filter - use recalled for that",
            "in": "query",
            "required": false,
            "type": "integer",
            "format": "int32"
          },
          {
            "name": "recallCountMax",
            "description": "inclusive upper bound on recall_count; 0 (the default) means no bound, NOT a literal never-recalled filter - use recalled for that",
            "in": "query",
            "required": false,
            "type": "integer",
            "format": "int32"
          },
          {
            "name": "timeRecalledMin",
            "description": "UnixNano inclusive lower bound on time_recalled; 0 (the default) means no lower bound. Both bounds ask only about memories that HAVE been recalled - see time_recalled_max",
            "in": "query",
            "required": false,
            "type": "string",
            "format": "int64"
          },
          {
            "name": "timeRecalledMax",
            "description": "UnixNano inclusive upper bound on time_recalled; 0 (the default) means no upper bound. Never-recalled memories are excluded rather than matched: their time_recalled is 0, so \"recalled before X\" would otherwise return every memory that was never recalled at all. Use recalled to ask about those",
            "in": "query",
            "required": false,
            "type": "string",
            "format": "int64"
          },
          {
            "name": "isSummary",
            "description": "optional: TRUE returns only summary memories (see ReplaceMemoriesWithSummary), FALSE only non-summaries; UNSPECIFIED (default) applies no restriction\n\n - UNSPECIFIED: not set; on Memory.is_binary the server treats this the same as FALSE, on a filter it applies no restriction\n - FALSE: explicitly false\n - TRUE: explicitly true",
            "in": "query",
            "required": false,
            "type": "string",
            "enum": [
              "UNSPECIFIED",
              "FALSE",
              "TRUE"
            ],
            "default": "UNSPECIFIED"
          },
          {
            "name": "isBinary",
            "description": "optional: TRUE returns only binary memories, FALSE only non-binary; UNSPECIFIED (default) applies no restriction\n\n - UNSPECIFIED: not set; on Memory.is_binary the server treats this the same as FALSE, on a filter it applies no restriction\n - FALSE: explicitly false\n - TRUE: explicitly true",
            "in": "query",
            "required": false,
            "type": "string",
            "enum": [
              "UNSPECIFIED",
              "FALSE",
              "TRUE"
            ],
            "default": "UNSPECIFIED"
          },
          {
            "name": "eventId",
            "description": "optional: restrict to memories associated with this event. This is the paged way to read an event's memories - GetEventById with memories: true returns them all in one message, which overruns the receive frame on a large event. Empty (the default) applies no restriction rather than matching the event-less; use has_event for those",
            "in": "query",
            "required": false,
            "type": "string"
          },
          {
            "name": "hasEvent",
            "description": "optional: FALSE returns only memories associated with no event, TRUE only those associated with one; UNSPECIFIED (default) applies no restriction. Exists because event_id cannot say it - an event-less memory stores an empty event_id, which is the same value that means \"no bound\" there, exactly as recalled exists alongside recall_count_min/max\n\n - UNSPECIFIED: not set; on Memory.is_binary the server treats this the same as FALSE, on a filter it applies no restriction\n - FALSE: explicitly false\n - TRUE: explicitly true",
            "in": "query",
            "required": false,
            "type": "string",
            "enum": [
              "UNSPECIFIED",
              "FALSE",
              "TRUE"
            ],
            "default": "UNSPECIFIED"
          },
          {
            "name": "orderDir",
            "description": "optional: reverse order_by's sort direction; UNSPECIFIED (the default) uses that field's natural direction. Applies to the whole ordering, tiebreakers included, so ASC returns exactly the reverse of DESC\n\n - SORT_DIRECTION_UNSPECIFIED: sort in the order_by field's natural direction (see above)\n - SORT_DIRECTION_ASC: ascending, whatever the field's natural direction\n - SORT_DIRECTION_DESC: descending, whatever the field's natural direction",
            "in": "query",
            "required": false,
            "type": "string",
            "enum": [
              "SORT_DIRECTION_UNSPECIFIED",
              "SORT_DIRECTION_ASC",
              "SORT_DIRECTION_DESC"
            ],
            "default": "SORT_DIRECTION_UNSPECIFIED"
          }
        ],
        "tags": [
          "Hippocampus"
        ]
      },
      "post": {
        "summary": "Memories\nStoreMemory creates a memory. A memory below memory.minimumSignificance is quietly dropped -\nsee StoreMemoryResponse.",
        "operationId": "Hippocampus_StoreMemory",
        "responses": {
          "200": {
            "description": "A successful response.",
            "schema": {
              "$ref": "#/definitions/v1StoreMemoryResponse"
            }
          },
          "default": {
            "description": "An unexpected error response.",
            "schema": {
              "$ref": "#/definitions/rpcStatus"
            }
          }
        },
        "parameters": [
          {
            "name": "body",
            "description": "Memory is a blob, opaque to the server, with a significance and timestamp, optionally linked to\nan event. Recalling it (RecallMemories) reinforces it: the decay clock resets and its effective\nsignificance rises. is_binary controls whether the body is indexed for content search (see\nSearchMemories) - a binary body is never indexed.",
            "in": "body",
            "required": true,
            "schema": {
              "$ref": "#/definitions/v1Memory"
            }
          }
        ],
        "tags": [
          "Hippocampus"
        ]
      }
    },
    "/v1/memories/batch": {
      "post": {
        "summary": "StoreMemories creates many memories in one call - the write-path counterpart to ImportBatch,\nfor a producer holding a batch of unrelated memories (a broker bridge, a log pipeline) that\nwould otherwise send one StoreMemory per record.",
        "description": "Every memory goes through exactly the validation, defaulting and minimum-significance gate\nStoreMemory applies, and each is written in its own transaction: one bad record does not fail\nthe rest, and a partial success is the useful answer. It is NOT ImportBatch - nothing carries\nrecall history here, and nothing is upserted; a memory naming an id the store already holds\nfails with ALREADY_EXISTS in its own result rather than replacing a live row.\n\nresults is positional: results[i] describes memories[i]. The RPC itself fails only for a\nbatch-level fault (no memories, more than the cap, a cancelled context).",
        "operationId": "Hippocampus_StoreMemories",
        "responses": {
          "200": {
            "description": "A successful response.",
            "schema": {
              "$ref": "#/definitions/v1StoreMemoriesResponse"
            }
          },
          "default": {
            "description": "An unexpected error response.",
            "schema": {
              "$ref": "#/definitions/rpcStatus"
            }
          }
        },
        "parameters": [
          {
            "name": "body",
            "description": "StoreMemoriesRequest carries a batch of unrelated memories for the write path. Each is written\nindependently, so they need share nothing - not an event, not a group, not a significance.",
            "in": "body",
            "required": true,
            "schema": {
              "$ref": "#/definitions/v1StoreMemoriesRequest"
            }
          }
        ],
        "tags": [
          "Hippocampus"
        ]
      }
    },
    "/v1/memories/delete": {
      "post": {
        "summary": "DeleteMemories deletes the given memories by id; unknown ids are silently ignored.",
        "operationId": "Hippocampus_DeleteMemories",
        "responses": {
          "200": {
            "description": "A successful response.",
            "schema": {
              "$ref": "#/definitions/v1GeneralResponse"
            }
          },
          "default": {
            "description": "An unexpected error response.",
            "schema": {
              "$ref": "#/definitions/rpcStatus"
            }
          }
        },
        "parameters": [
          {
            "name": "body",
            "in": "body",
            "required": true,
            "schema": {
              "$ref": "#/definitions/v1DeleteMemoriesRequest"
            }
          }
        ],
        "tags": [
          "Hippocampus"
        ]
      }
    },
    "/v1/memories/delete-by-filter": {
      "post": {
        "summary": "DeleteMemoriesByFilter deletes every memory matching the filter - the same selection the\nGetMemories listing takes - rather than a list of ids. It is what offboarding a group, or\nanswering an erasure request, actually needs: the alternative is paging GetMemories and\ndeleting by id while the store changes underneath, which is not safer than this, only more\nwork.",
        "description": "A request carrying no filter at all is refused with INVALID_ARGUMENT: \"delete everything\" is\nPurge, and must not be reachable by leaving a field unset. To see what a call would remove,\nsend the same fields to GetMemories and read total_count - the two build the same predicate,\nwhich is a tested property rather than a convention.\n\nDeletion is permanent, and is the ordinary one: links are pruned and the search index is\nupdated in the same transaction the rows go in, exactly as DeleteMemories does. Like\nDeleteMemories it writes no forgotten-log tombstone - that log records what a decay cycle\ntook, and a caller that asked for a deletion already knows.",
        "operationId": "Hippocampus_DeleteMemoriesByFilter",
        "responses": {
          "200": {
            "description": "A successful response.",
            "schema": {
              "$ref": "#/definitions/v1DeleteMemoriesByFilterResponse"
            }
          },
          "default": {
            "description": "An unexpected error response.",
            "schema": {
              "$ref": "#/definitions/rpcStatus"
            }
          }
        },
        "parameters": [
          {
            "name": "body",
            "description": "DeleteMemoriesByFilterRequest selects the memories to delete. Every field below means exactly\nwhat the same field means on GetMemoriesRequest, so the listing is the dry run for the deletion.\n\nWhat is deliberately NOT here, and why:\n\n  - order_by/order_dir/limit/offset. There is no page to order or skip; the call deletes\n    everything the predicate matches. max_deletions below bounds one call's work instead.\n  - links. There is no memory in the response to attach them to.\n  - linked_to. Deleting a memory removes its edges, so that predicate narrows as a consequence of\n    its own deletions: what the listing showed the caller and what the call would remove are not\n    the same set, and no amount of care on the caller's part could make them agree.\n\nAt least one selecting field must be set. A request with none is refused rather than read as\n\"everything\" - that operation exists, is called Purge, and is not something a defaulted field\nshould be able to reach.",
            "in": "body",
            "required": true,
            "schema": {
              "$ref": "#/definitions/v1DeleteMemoriesByFilterRequest"
            }
          }
        ],
        "tags": [
          "Hippocampus"
        ]
      }
    },
    "/v1/memories/forgotten": {
      "get": {
        "summary": "GetForgottenMemories reads the forgotten log: one record per memory the sleep cycle actually\ndeleted, with the decision that took it. Where PreviewConsolidation answers \"what would go\"\nand ExplainConsolidation \"where does this memory stand\", this answers \"what went, and why\" -\nthe only one of the three that can speak about a memory that no longer exists.",
        "description": "It is optional and off by default (consolidation.tombstones.enabled): a store that is not\nrecording returns an empty log rather than an error, since \"nothing was recorded\" is the\nhonest answer to what was forgotten. Records carry no body - a tombstone reports that\nsomething was lost and is deliberately not an undelete.",
        "operationId": "Hippocampus_GetForgottenMemories",
        "responses": {
          "200": {
            "description": "A successful response.",
            "schema": {
              "$ref": "#/definitions/v1GetForgottenMemoriesResponse"
            }
          },
          "default": {
            "description": "An unexpected error response.",
            "schema": {
              "$ref": "#/definitions/rpcStatus"
            }
          }
        },
        "parameters": [
          {
            "name": "memoryId",
            "description": "a specific memory - \"did this exist, and when did it go\"",
            "in": "query",
            "required": false,
            "type": "string"
          },
          {
            "name": "eventId",
            "in": "query",
            "required": false,
            "type": "string"
          },
          {
            "name": "group",
            "in": "query",
            "required": false,
            "type": "string"
          },
          {
            "name": "rule",
            "description": "UNSPECIFIED returns both rules\n\n - FORGET_RULE_CONSOLIDATION: decayed value fell below the capacity-pressure-scaled deletion threshold\n - FORGET_RULE_EVICTION: still above the threshold, but evicted to bring the store back under its byte capacity",
            "in": "query",
            "required": false,
            "type": "string",
            "enum": [
              "FORGET_RULE_UNSPECIFIED",
              "FORGET_RULE_CONSOLIDATION",
              "FORGET_RULE_EVICTION"
            ],
            "default": "FORGET_RULE_UNSPECIFIED"
          },
          {
            "name": "since",
            "description": "UnixNano, inclusive lower bound on forgotten_at",
            "in": "query",
            "required": false,
            "type": "string",
            "format": "int64"
          },
          {
            "name": "until",
            "description": "UnixNano, exclusive upper bound on forgotten_at",
            "in": "query",
            "required": false,
            "type": "string",
            "format": "int64"
          },
          {
            "name": "afterSeq",
            "description": "pagination: return records below this seq (pass the lowest seq of the previous page)",
            "in": "query",
            "required": false,
            "type": "string",
            "format": "int64"
          },
          {
            "name": "limit",
            "description": "\u003c= 0 selects the default (100), values above the cap (1000) are clamped down to it",
            "in": "query",
            "required": false,
            "type": "integer",
            "format": "int32"
          }
        ],
        "tags": [
          "Hippocampus"
        ]
      }
    },
    "/v1/memories/forgotten/delete": {
      "post": {
        "summary": "DeleteForgottenMemories empties the forgotten log, or the part of it older than before_time.",
        "description": "It exists because the log's automatic bounds (consolidation.tombstones.maxRows/maxAgeInDays)\nonly apply while recording is enabled: turning the feature off deliberately leaves what was\nalready recorded in place, so that a configuration change never destroys a record somebody\nkept. Removing it is therefore always an explicit request - this one.",
        "operationId": "Hippocampus_DeleteForgottenMemories",
        "responses": {
          "200": {
            "description": "A successful response.",
            "schema": {
              "$ref": "#/definitions/v1DeleteForgottenMemoriesResponse"
            }
          },
          "default": {
            "description": "An unexpected error response.",
            "schema": {
              "$ref": "#/definitions/rpcStatus"
            }
          }
        },
        "parameters": [
          {
            "name": "body",
            "description": "DeleteForgottenMemoriesRequest empties the forgotten log. Supply before_time to delete only what\nwas forgotten before it, or all to empty the log entirely - one of the two is required, so an\nempty request can never silently discard the whole log.",
            "in": "body",
            "required": true,
            "schema": {
              "$ref": "#/definitions/v1DeleteForgottenMemoriesRequest"
            }
          }
        ],
        "tags": [
          "Hippocampus"
        ]
      }
    },
    "/v1/memories/recall": {
      "post": {
        "summary": "RecallMemories returns the requested memories and reinforces each one: its recall time is\nreset to now (resetting its decay clock) and its recall count is incremented (raising its\neffective significance during consolidation). A reader-tier caller for whom reinforcement is\ndisabled (auth.readerRecallReinforces) gets a plain, non-reinforcing read instead; writer and\nadmin callers always reinforce.",
        "operationId": "Hippocampus_RecallMemories",
        "responses": {
          "200": {
            "description": "A successful response.",
            "schema": {
              "$ref": "#/definitions/v1GetMemoriesResponse"
            }
          },
          "default": {
            "description": "An unexpected error response.",
            "schema": {
              "$ref": "#/definitions/rpcStatus"
            }
          }
        },
        "parameters": [
          {
            "name": "body",
            "in": "body",
            "required": true,
            "schema": {
              "$ref": "#/definitions/v1RecallMemoriesRequest"
            }
          }
        ],
        "tags": [
          "Hippocampus"
        ]
      }
    },
    "/v1/memories/search": {
      "post": {
        "summary": "SearchMemories finds memories via the optional content-search index; see\nSearchMemoriesRequest.",
        "operationId": "Hippocampus_SearchMemories",
        "responses": {
          "200": {
            "description": "A successful response.",
            "schema": {
              "$ref": "#/definitions/v1GetMemoriesResponse"
            }
          },
          "default": {
            "description": "An unexpected error response.",
            "schema": {
              "$ref": "#/definitions/rpcStatus"
            }
          }
        },
        "parameters": [
          {
            "name": "body",
            "description": "SearchMemoriesRequest queries the secondary content-search index for memories matching the\nquery. Results are always re-read from the primary store, which remains authoritative; ids the\nindex returns that the primary store no longer holds are silently dropped. When reinforce is\ntrue the matched memories are recalled - resetting their decay clocks and raising their\neffective significance - rather than merely fetched, and only the memories actually returned are\nreinforced.",
            "in": "body",
            "required": true,
            "schema": {
              "$ref": "#/definitions/v1SearchMemoriesRequest"
            }
          }
        ],
        "tags": [
          "Hippocampus"
        ]
      }
    },
    "/v1/memories/{id}": {
      "patch": {
        "summary": "UpdateMemory applies a partial update to an existing memory: only the fields carrying a value\n(significance, body, event_id, group, time_stamp, metadata) overwrite the stored row. is_binary and\nis_summary are NOT updatable here - they are set at creation and by ReplaceMemoriesWithSummary\n- so any updated content must stay consistent with the memory's existing is_binary/is_summary:\na non-binary memory is re-indexed for content search, so its new body must remain valid text,\nand a summary memory should keep summary-appropriate content. An unknown id returns NotFound\n(no memory is created).",
        "operationId": "Hippocampus_UpdateMemory",
        "responses": {
          "200": {
            "description": "A successful response.",
            "schema": {
              "$ref": "#/definitions/v1GeneralResponse"
            }
          },
          "default": {
            "description": "An unexpected error response.",
            "schema": {
              "$ref": "#/definitions/rpcStatus"
            }
          }
        },
        "parameters": [
          {
            "name": "id",
            "description": "auto-generated UUID if omitted on create; max 128 characters",
            "in": "path",
            "required": true,
            "type": "string"
          },
          {
            "name": "body",
            "in": "body",
            "required": true,
            "schema": {
              "$ref": "#/definitions/HippocampusUpdateMemoryBody"
            }
          }
        ],
        "tags": [
          "Hippocampus"
        ]
      }
    },
    "/v1/memories/{id}/links": {
      "get": {
        "summary": "GetMemoryLinks returns a memory's links. By default both directions are returned (the graph is\nstored directed but valued symmetrically); set direction to narrow it. NotFound if the memory\ndoes not exist.",
        "operationId": "Hippocampus_GetMemoryLinks",
        "responses": {
          "200": {
            "description": "A successful response.",
            "schema": {
              "$ref": "#/definitions/v1GetLinksResponse"
            }
          },
          "default": {
            "description": "An unexpected error response.",
            "schema": {
              "$ref": "#/definitions/rpcStatus"
            }
          }
        },
        "parameters": [
          {
            "name": "id",
            "description": "the memory whose links to return; must exist",
            "in": "path",
            "required": true,
            "type": "string"
          },
          {
            "name": "direction",
            "description": "which directions to return; unset means BOTH\n\n - LINK_DIRECTION_UNSPECIFIED: treated as BOTH\n - LINK_DIRECTION_BOTH: links in either direction\n - LINK_DIRECTION_OUTBOUND: only links this item declared, pointing away from it\n - LINK_DIRECTION_INBOUND: only links other items declared, pointing at it",
            "in": "query",
            "required": false,
            "type": "string",
            "enum": [
              "LINK_DIRECTION_UNSPECIFIED",
              "LINK_DIRECTION_BOTH",
              "LINK_DIRECTION_OUTBOUND",
              "LINK_DIRECTION_INBOUND"
            ],
            "default": "LINK_DIRECTION_UNSPECIFIED"
          }
        ],
        "tags": [
          "Hippocampus"
        ]
      },
      "post": {
        "summary": "LinkMemories adds or updates links from one memory to others. Re-linking an existing pair\noverwrites that link's significance rather than adding a second link. NotFound if the memory\nor any target does not exist; InvalidArgument on a self-link or once the memory would exceed\nthe per-item link limit.",
        "operationId": "Hippocampus_LinkMemories",
        "responses": {
          "200": {
            "description": "A successful response.",
            "schema": {
              "$ref": "#/definitions/v1GeneralResponse"
            }
          },
          "default": {
            "description": "An unexpected error response.",
            "schema": {
              "$ref": "#/definitions/rpcStatus"
            }
          }
        },
        "parameters": [
          {
            "name": "id",
            "description": "the memory the links start from; must exist",
            "in": "path",
            "required": true,
            "type": "string"
          },
          {
            "name": "body",
            "in": "body",
            "required": true,
            "schema": {
              "$ref": "#/definitions/HippocampusLinkMemoriesBody"
            }
          }
        ],
        "tags": [
          "Hippocampus"
        ]
      }
    },
    "/v1/memories/{id}/links/delete": {
      "post": {
        "summary": "UnlinkMemories removes links from one memory to the named memories; unknown targets are\nsilently ignored. NotFound if the memory itself does not exist.",
        "operationId": "Hippocampus_UnlinkMemories",
        "responses": {
          "200": {
            "description": "A successful response.",
            "schema": {
              "$ref": "#/definitions/v1GeneralResponse"
            }
          },
          "default": {
            "description": "An unexpected error response.",
            "schema": {
              "$ref": "#/definitions/rpcStatus"
            }
          }
        },
        "parameters": [
          {
            "name": "id",
            "description": "the memory the links start from; must exist",
            "in": "path",
            "required": true,
            "type": "string"
          },
          {
            "name": "body",
            "in": "body",
            "required": true,
            "schema": {
              "$ref": "#/definitions/HippocampusUnlinkMemoriesBody"
            }
          }
        ],
        "tags": [
          "Hippocampus"
        ]
      }
    },
    "/v1/purge": {
      "post": {
        "summary": "Purge deletes every event and memory in the store. While it runs, all other Hippocampus RPCs\nare rejected with Unavailable.",
        "operationId": "Hippocampus_Purge",
        "responses": {
          "200": {
            "description": "A successful response.",
            "schema": {
              "$ref": "#/definitions/v1GeneralResponse"
            }
          },
          "default": {
            "description": "An unexpected error response.",
            "schema": {
              "$ref": "#/definitions/rpcStatus"
            }
          }
        },
        "tags": [
          "Hippocampus"
        ]
      }
    },
    "/v1/significance/levels": {
      "get": {
        "summary": "GetSignificanceLevels lists the distinct significance values currently in use - the\nsignificance registry, which is the scale SignificancePlacement positions against. Without it\na client using placement is choosing anchors it cannot see, and a client choosing an absolute\nsignificance is guessing at the scheme a store already uses.",
        "description": "It names no stored record: one shared registry ranks memories and events alike, so a value is\nstore-global and says nothing about who carries it. That is why it is reader tier and why a\ngroup-scoped caller is answered in full rather than refused - there is nothing here to\npartition.",
        "operationId": "Hippocampus_GetSignificanceLevels",
        "responses": {
          "200": {
            "description": "A successful response.",
            "schema": {
              "$ref": "#/definitions/v1GetSignificanceLevelsResponse"
            }
          },
          "default": {
            "description": "An unexpected error response.",
            "schema": {
              "$ref": "#/definitions/rpcStatus"
            }
          }
        },
        "parameters": [
          {
            "name": "significanceMin",
            "description": "inclusive lower bound; 0 (the default) means no lower bound, NOT a literal significance-of-zero filter",
            "in": "query",
            "required": false,
            "type": "integer",
            "format": "int32"
          },
          {
            "name": "significanceMax",
            "description": "inclusive upper bound; 0 (the default) means no upper bound",
            "in": "query",
            "required": false,
            "type": "integer",
            "format": "int32"
          },
          {
            "name": "limit",
            "description": "page size; a value \u003c= 0 selects the server default (200); values above the server cap (1000) are silently clamped down to it",
            "in": "query",
            "required": false,
            "type": "integer",
            "format": "int32"
          },
          {
            "name": "offset",
            "description": "rows to skip for pagination; negative values are clamped to 0; no upper bound",
            "in": "query",
            "required": false,
            "type": "integer",
            "format": "int32"
          }
        ],
        "tags": [
          "Hippocampus"
        ]
      }
    },
    "/v1/sleep": {
      "post": {
        "summary": "Sleep manually triggers a consolidation cycle out of schedule and resets the periodic timer.\nRejected with FailedPrecondition on a read/write replica (consolidation.enabled: false),\nwhich must never run its own cycle.",
        "operationId": "Hippocampus_Sleep",
        "responses": {
          "200": {
            "description": "A successful response.",
            "schema": {
              "$ref": "#/definitions/v1GeneralResponse"
            }
          },
          "default": {
            "description": "An unexpected error response.",
            "schema": {
              "$ref": "#/definitions/rpcStatus"
            }
          }
        },
        "tags": [
          "Hippocampus"
        ]
      }
    },
    "/v1/sleep/preview": {
      "get": {
        "summary": "PreviewConsolidation reports what a consolidation cycle would forget if one ran now, and\ndeletes nothing. It is the dry run for Sleep: the same decision rules, applied to the same\nstore, with the outcome returned instead of applied. Rejected with FailedPrecondition on a\nread/write replica (consolidation.enabled: false), which never runs a cycle of its own and so\nhas nothing to preview.",
        "description": "It is a separate RPC rather than a flag on Sleep so that the two can be authorised\nseparately - previewing what would be forgotten is a read, triggering it is not.",
        "operationId": "Hippocampus_PreviewConsolidation",
        "responses": {
          "200": {
            "description": "A successful response.",
            "schema": {
              "$ref": "#/definitions/v1PreviewConsolidationResponse"
            }
          },
          "default": {
            "description": "An unexpected error response.",
            "schema": {
              "$ref": "#/definitions/rpcStatus"
            }
          }
        },
        "parameters": [
          {
            "name": "limit",
            "description": "memories to detail; \u003c= 0 selects the default (100), values above the cap (1000) are clamped down to it",
            "in": "query",
            "required": false,
            "type": "integer",
            "format": "int32"
          }
        ],
        "tags": [
          "Hippocampus"
        ]
      }
    },
    "/v1/summarisation/candidates": {
      "get": {
        "summary": "GetSummarisationCandidates returns events identified by the most recent sleep cycle as having\naccumulated enough quiet, unsummarised memories to be worth condensing via\nReplaceMemoriesWithSummary. A point-in-time snapshot, refreshed only when\nconsolidation.summarisationMinMemories is configured.",
        "operationId": "Hippocampus_GetSummarisationCandidates",
        "responses": {
          "200": {
            "description": "A successful response.",
            "schema": {
              "$ref": "#/definitions/v1GetSummarisationCandidatesResponse"
            }
          },
          "default": {
            "description": "An unexpected error response.",
            "schema": {
              "$ref": "#/definitions/rpcStatus"
            }
          }
        },
        "tags": [
          "Hippocampus"
        ]
      }
    },
    "/v1/topology": {
      "get": {
        "summary": "GetTopology reports the deployment as this instance understands it: itself, the components it\ndials, how they relate, and the last known health of each. Where WhoAmI answers \"who am I\ntalking as\", this answers \"what am I talking to\".",
        "description": "It reports only what an instance can honestly know - itself and its outbound dependencies.\nEverything else in a deployment dials IN and is invisible unless declared, which is what\nTopologyNodeSource exists to say on every node. This is deliberately NOT a control plane: the\nresponse is read-only and there is no counterpart that acts on another component.\n\nThe minimum tier is configurable (topology.minimumTier, default reader) because how sensitive\na deployment's internal shape is depends on the deployment. What is NOT configurable is the\nrefusal to a group-scoped caller: that is not a sensitivity judgement but the same one behind\nPurge and PreviewConsolidation - the answer cannot be partitioned, since there is no per-group\ntopology, so a scoped caller could only be shown infrastructure that is not theirs.\n\nNo secret ever reaches the response. Addresses are redacted to scheme, host, port and\ndatabase/bucket name; DSN credentials, signing keys, and passwords are never included, in any\ntier, for any caller.",
        "operationId": "Hippocampus_GetTopology",
        "responses": {
          "200": {
            "description": "A successful response.",
            "schema": {
              "$ref": "#/definitions/v1GetTopologyResponse"
            }
          },
          "default": {
            "description": "An unexpected error response.",
            "schema": {
              "$ref": "#/definitions/rpcStatus"
            }
          }
        },
        "tags": [
          "Hippocampus"
        ]
      }
    },
    "/v1/transfer": {
      "post": {
        "summary": "Transfer streams the whole store directly into a centralised instance's ImportBatch RPC,\nrecording a manifest like Export; with clear set the captured records are deleted once the\ntarget has accepted every batch.",
        "operationId": "Hippocampus_Transfer",
        "responses": {
          "200": {
            "description": "A successful response.",
            "schema": {
              "$ref": "#/definitions/v1TransferResponse"
            }
          },
          "default": {
            "description": "An unexpected error response.",
            "schema": {
              "$ref": "#/definitions/rpcStatus"
            }
          }
        },
        "parameters": [
          {
            "name": "body",
            "description": "Transfer streams every event and memory directly to a centralised instance's ImportBatch RPC\n(address from transfer.targetAddress); like Export it records a manifest, and with clear set\ndeletes the captured records once the target has accepted them all.",
            "in": "body",
            "required": true,
            "schema": {
              "$ref": "#/definitions/v1TransferRequest"
            }
          }
        ],
        "tags": [
          "Hippocampus"
        ]
      }
    },
    "/v1/whoami": {
      "get": {
        "summary": "WhoAmI reports the authenticated caller's identity and effective authorization tier\n(reader/writer/admin), so a client - the web console - can tailor what it offers instead of\nguessing at the token's roles. Requires only the reader tier. When the service runs without\nauthentication it reports auth_enabled false and an unrestricted (admin) tier.",
        "operationId": "Hippocampus_WhoAmI",
        "responses": {
          "200": {
            "description": "A successful response.",
            "schema": {
              "$ref": "#/definitions/v1WhoAmIResponse"
            }
          },
          "default": {
            "description": "An unexpected error response.",
            "schema": {
              "$ref": "#/definitions/rpcStatus"
            }
          }
        },
        "tags": [
          "Hippocampus"
        ]
      }
    }
  },
  "definitions": {
    "HippocampusEndEventBody": {
      "type": "object",
      "properties": {
        "timeEnd": {
          "type": "string",
          "format": "int64",
          "title": "UnixNano; 0 (the default) uses the current server time"
        }
      }
    },
    "HippocampusLinkEventsBody": {
      "type": "object",
      "properties": {
        "links": {
          "type": "array",
          "items": {
            "type": "object",
            "$ref": "#/definitions/v1Link"
          },
          "title": "the targets and their significances; each target must exist and must not be id itself"
        }
      }
    },
    "HippocampusLinkMemoriesBody": {
      "type": "object",
      "properties": {
        "links": {
          "type": "array",
          "items": {
            "type": "object",
            "$ref": "#/definitions/v1Link"
          },
          "title": "the targets and their significances; each target must exist and must not be id itself"
        }
      },
      "description": "LinkMemoriesRequest adds or updates links from one memory to others. It is an upsert per pair:\nre-linking an existing pair overwrites that link's significance rather than creating a duplicate."
    },
    "HippocampusSummariseMemoriesBody": {
      "type": "object",
      "properties": {
        "significance": {
          "type": "integer",
          "format": "int32",
          "title": "optional: significance for the resulting summary memory; 0 (and no placement) defaults to the highest significance among the replaced memories"
        },
        "placement": {
          "$ref": "#/definitions/v1SignificancePlacement",
          "title": "optional: rank the summary relative to existing values (see SignificancePlacement); takes precedence over significance when set"
        }
      },
      "description": "SummariseMemoriesRequest asks the service to generate a summary of an event's memories with the\nembedded LLM (llm.enabled) and replace them with it in one transaction, like\nReplaceMemoriesWithSummary but with the service authoring the summary. Binary memories are\nexcluded from the LLM prompt (their bodies are opaque, not text)."
    },
    "HippocampusUnlinkEventsBody": {
      "type": "object",
      "properties": {
        "ids": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "title": "targets to unlink; unknown ids are silently ignored. Removes links in either direction between id and each target"
        }
      }
    },
    "HippocampusUnlinkMemoriesBody": {
      "type": "object",
      "properties": {
        "ids": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "title": "targets to unlink; unknown ids are silently ignored. Removes links in either direction between id and each target"
        }
      }
    },
    "HippocampusUpdateEventBody": {
      "type": "object",
      "properties": {
        "timeStart": {
          "type": "string",
          "format": "int64",
          "title": "UnixNano; defaults to the current time when unset (0) on create; required (\u003e 0) once defaulted"
        },
        "timeEnd": {
          "type": "string",
          "format": "int64",
          "title": "UnixNano; 0 means the event has not ended yet (see EndEvent); when set, must not be before time_start"
        },
        "significance": {
          "type": "integer",
          "format": "int32",
          "title": "0 = unranked (rank it later via UpdateEventSignificance/placement); must be \u003e= 0; higher ranks more significant"
        },
        "name": {
          "type": "string",
          "title": "required (non-empty) on create; max 256 characters"
        },
        "description": {
          "type": "string",
          "title": "optional; max 1024 characters"
        },
        "memoriesConsolidated": {
          "type": "boolean",
          "title": "read-only: true once a sleep cycle has deleted (consolidated) some of this event's memories; ignored on write"
        },
        "linkSignificance": {
          "type": "string",
          "format": "int64",
          "title": "read-only: sum of this event's links' significance values, inbound and outbound, calculated by the server; ignored on write"
        },
        "links": {
          "type": "array",
          "items": {
            "type": "object",
            "$ref": "#/definitions/v1Link"
          },
          "title": "links this event declares to other events, weighted into its memories' value (see consolidation.linkSignificanceWeight). Outbound only, like Memory.links - ask GetEventLinks for both directions"
        },
        "memories": {
          "type": "array",
          "items": {
            "type": "object",
            "$ref": "#/definitions/v1Memory"
          },
          "title": "populated on read only when requested (GetEventById/GetEvents); write-only input on StoreEvent, stored via the same rules as StoreMemory"
        },
        "group": {
          "type": "string",
          "title": "optional freeform grouping/context label (system, subsystem, owner, ...); max 128 characters"
        },
        "placement": {
          "$ref": "#/definitions/v1SignificancePlacement",
          "title": "write-only: rank relative to existing values (see SignificancePlacement)"
        },
        "metadata": {
          "type": "object",
          "additionalProperties": {
            "type": "string"
          },
          "title": "optional multi-dimensional classification, exactly as Memory.metadata: same key charset, same 32-key/512-byte-value/4096-byte bounds, same replace-wholesale semantics on update, same opacity to the server"
        },
        "clearMetadata": {
          "type": "boolean",
          "title": "write-only: on UpdateEvent, remove all metadata (see Memory.clear_metadata for why the empty map cannot say it)"
        },
        "clearGroup": {
          "type": "boolean",
          "title": "write-only: on UpdateEvent, reset group to empty (see Memory.clear_group)"
        },
        "memoryCount": {
          "type": "integer",
          "format": "int32",
          "title": "read-only: memories currently attached to this event, populated only when asked for (GetEventsRequest.memory_counts, GetEventByIdRequest.memory_counts - see there for why it is opt-in); 0 otherwise, which is indistinguishable from an event holding none, so ask for the count if you need to tell them apart. Counted within the caller's group scope, exactly as the nested memories are filtered. Ignored on write"
        }
      },
      "description": "Event is a named time span with its own significance, optionally carrying links to\nother events and/or nested memories. memories/links are populated on read only when\nrequested (GetEventById, GetEvents); on StoreEvent nested memories are write-only input, each\nstored via the same rules as StoreMemory."
    },
    "HippocampusUpdateEventSignificanceBody": {
      "type": "object",
      "properties": {
        "significance": {
          "type": "integer",
          "format": "int32",
          "title": "the new absolute significance; must be \u003e= 1 to take effect. IMPORTANT: 0 with no placement is a no-op - it leaves the event's existing significance unchanged; there is no way to reset an event to unranked via this RPC"
        },
        "placement": {
          "$ref": "#/definitions/v1SignificancePlacement",
          "title": "write-only: rank relative to existing values (see SignificancePlacement); takes precedence over significance when set"
        }
      }
    },
    "HippocampusUpdateMemoryBody": {
      "type": "object",
      "properties": {
        "timeStamp": {
          "type": "string",
          "format": "int64",
          "title": "UnixNano; defaults to the current time when unset (0) on create; required (\u003e 0) once defaulted"
        },
        "significance": {
          "type": "integer",
          "format": "int32",
          "title": "0 = unranked (rank it later via UpdateMemory/placement); must be \u003e= 0; higher ranks more significant. On UpdateMemory, 0 with no placement leaves the existing significance unchanged - it cannot reset a memory to unranked"
        },
        "eventId": {
          "type": "string",
          "title": "optional: leave unset to store the memory unassociated with any event"
        },
        "body": {
          "type": "string",
          "title": "required (non-empty) on create; a proto3 string, so always valid UTF-8 on the wire - encode true binary payloads (e.g. base64) before setting is_binary; optionally capped by memory.limit.sizeBytes (0 = unlimited)"
        },
        "isBinary": {
          "$ref": "#/definitions/v1Bool",
          "title": "set once at creation; not updatable via UpdateMemory. TRUE bodies are opaque and never indexed for content search (see SearchMemories)"
        },
        "timeRecalled": {
          "type": "string",
          "format": "int64",
          "title": "read-only: UnixNano of the most recent recall (RecallMemories, or SearchMemories with reinforce); 0 if never recalled; ignored on write"
        },
        "recallCount": {
          "type": "integer",
          "format": "int32",
          "title": "read-only: number of times the memory has been recalled; ignored on write"
        },
        "isSummary": {
          "type": "boolean",
          "title": "read-only: true for the summary memory created by ReplaceMemoriesWithSummary; not updatable via UpdateMemory"
        },
        "group": {
          "type": "string",
          "title": "optional freeform grouping/context label (system, subsystem, owner, ...); max 128 characters"
        },
        "placement": {
          "$ref": "#/definitions/v1SignificancePlacement",
          "title": "write-only: rank relative to existing values (see SignificancePlacement)"
        },
        "links": {
          "type": "array",
          "items": {
            "type": "object",
            "$ref": "#/definitions/v1Link"
          },
          "title": "associative links this memory declares, weighted into its value (see consolidation.linkSignificanceWeight). Outbound only - Link is a directed edge from the item carrying it, so this lists exactly the edges this memory owns and an export/import round trip reproduces them once each; ask GetMemoryLinks for both directions. Write-only on StoreMemory (targets must already exist) and populated on read only when requested; use LinkMemories/UnlinkMemories to edit them incrementally"
        },
        "linkSignificance": {
          "type": "string",
          "format": "int64",
          "title": "read-only: sum of this memory's links' significance values, inbound and outbound, calculated by the server; ignored on write"
        },
        "metadata": {
          "type": "object",
          "additionalProperties": {
            "type": "string"
          },
          "title": "optional multi-dimensional classification (source=slack, project=..., author=...), where group is the single freeform label. Keys match [A-Za-z0-9][A-Za-z0-9._:/-]{0,63}; values are any UTF-8 up to 512 bytes; at most 32 keys and 4096 bytes of serialised JSON in total, and those bytes count toward memory.limit.sizeBytes alongside the body. On UpdateMemory a non-empty map REPLACES the stored map wholesale - there is no per-key merge, and an absent or empty map leaves it untouched (use clear_metadata to remove it). Opaque to the server: filterable via GetMemories/SearchMemories, never interpreted, and never emitted as a metric, span, or log attribute"
        },
        "clearMetadata": {
          "type": "boolean",
          "title": "write-only: on UpdateMemory, remove all metadata. Exists because the map's own emptiness cannot say it - an absent map and an explicitly empty one are the same on the wire, and every other updatable field reads unset as \"leave unchanged\". Ignored on StoreMemory; takes precedence over any metadata supplied alongside it"
        },
        "clearGroup": {
          "type": "boolean",
          "title": "write-only: on UpdateMemory, reset group to empty, for the same reason clear_metadata exists - an empty group string otherwise means \"leave unchanged\". Ignored on StoreMemory; takes precedence over any group supplied alongside it"
        },
        "externalBytes": {
          "type": "string",
          "format": "int64",
          "description": "external_bytes is the size of a payload this memory POINTS AT rather than holds: the trace,\nblob or document that lives in another system and that this memory's forgetting is meant to\ngovern. It is client-supplied and never interpreted - the service does not fetch, verify or\neven know the address of whatever it measures - and it exists for one reason: capacity\npressure is the control this store has that an age-based expiry does not, and pressure\ncomputed over a couple of hundred bytes of pointer while the payload behind it is forty\nkilobytes is regulating a quantity with no relationship to the resource it exists to protect.\n\nIt is a THIRD capacity axis (consolidation.capacityExternalBytes), never folded into\nused_bytes: used_bytes is this store's own footprint and protects its own disk, and\nconflating the two would let external volume evict memories to reclaim space it is not using.\nEviction counts it toward what a pass has reclaimed, but the ordering is unchanged - least\nvaluable still goes first, not worst value density.\n\nMust be \u003e= 0. On UpdateMemory 0 leaves the stored value unchanged, exactly as significance\ndoes, so a partial update of other fields cannot silently zero it; a memory whose payload is\ngone is a memory to delete rather than one to set back to zero."
        }
      },
      "description": "Memory is a blob, opaque to the server, with a significance and timestamp, optionally linked to\nan event. Recalling it (RecallMemories) reinforces it: the decay clock resets and its effective\nsignificance rises. is_binary controls whether the body is indexed for content search (see\nSearchMemories) - a binary body is never indexed."
    },
    "SignificancePlacementMode": {
      "type": "string",
      "enum": [
        "UNSPECIFIED",
        "ABOVE",
        "BELOW",
        "BETWEEN"
      ],
      "default": "UNSPECIFIED",
      "description": "- UNSPECIFIED: ignore placement; use the absolute significance value\n - ABOVE: rank just above the anchor, below the next-higher value\n - BELOW: rank just below the anchor, above the next-lower value\n - BETWEEN: rank strictly between the anchor (lower) and upper bounds"
    },
    "protobufAny": {
      "type": "object",
      "properties": {
        "@type": {
          "type": "string"
        }
      },
      "additionalProperties": {}
    },
    "rpcStatus": {
      "type": "object",
      "properties": {
        "code": {
          "type": "integer",
          "format": "int32"
        },
        "message": {
          "type": "string"
        },
        "details": {
          "type": "array",
          "items": {
            "type": "object",
            "$ref": "#/definitions/protobufAny"
          }
        }
      }
    },
    "v1AncillaryStorage": {
      "type": "object",
      "properties": {
        "measuredAt": {
          "type": "string",
          "format": "int64",
          "title": "UnixNano the cycle took this measurement"
        },
        "totalBytes": {
          "type": "string",
          "format": "int64"
        },
        "forgottenLog": {
          "$ref": "#/definitions/v1AncillaryTable"
        },
        "searchOutbox": {
          "$ref": "#/definitions/v1AncillaryTable"
        },
        "callbackQueue": {
          "$ref": "#/definitions/v1AncillaryTable"
        }
      },
      "description": "AncillaryStorage is the storage this store spends that its byte capacity target cannot see.\n\nThe three tables are excluded from used_bytes deliberately: the record of what was deleted must\nnever evict live memories, the search outbox is the sharp version of that (it grows precisely\nwhen deletions are backing up), and the callback queue is sharper again - it grows when a\nreceiver is down, and with callbacks.includeBodies it carries memory bodies. On the embedded\ndeployment all three share the store's own file, so the growth is real disk that capacity\npressure will never reflect: the disk a deployment needs is consolidation.capacityBytes plus\nthis.\n\nThe callback queue is the one to watch. Its rows have no fixed size, so callbacks.maxRows bounds\nits bytes only loosely, while the other two are fixed-width rows whose row caps are byte caps.\ncallbacks.maxBytes is what bounds it in the same unit the figure is reported in."
    },
    "v1AncillaryTable": {
      "type": "object",
      "properties": {
        "enabled": {
          "type": "boolean"
        },
        "rows": {
          "type": "string",
          "format": "int64"
        },
        "bytes": {
          "type": "string",
          "format": "int64"
        },
        "limitBytes": {
          "type": "string",
          "format": "int64",
          "description": "limit_bytes is the byte cap configured for this table, or 0 where none is - so bytes has the\nbound it is approaching beside it rather than being a figure with nothing to read it against.\nReaching the cap discards rows: for the two queues that is undelivered work (a notification\nnobody will receive, an index deletion left to the reverse sweep), and for the forgotten log\nit is the oldest end of a record.\n\nA cap of 0 is not \"no bytes allowed\": it is what every deployment has until an operator sets\none, since how much of this a deployment can afford is a property of its disk rather than of\nthe service. What bounds it meanwhile is the row cap, exactly - the rows being fixed width -\nfor the first two, and only loosely for the callback queue."
        }
      },
      "description": "AncillaryTable is what one of the three tables excluded from the capacity target holds.\n\nFor the two fixed-width tables bytes is rows multiplied by a flat per-row allowance, not a\nmeasurement - scanning them to add up what their count already says would put a cost on the path\nthat exists to bound the store - so rows is reported beside it rather than leaving a lone estimate\nto look more precise than it is. The callback queue is the exception, and is why the pair is\nreported: its rows carry a rendered payload and, under callbacks.includeBodies, memory bodies, so\na count says nothing about its size and its bytes are summed from a size written at insert.\n\nenabled separates the two zeroes: a feature nobody turned on holds nothing because nothing writes\nto it, while an enabled one holding nothing is a queue that is keeping up. They call for opposite\nreactions, and a bare 0 says neither.\n\nIt means \"is recording into this table right now\", NOT \"has rows worth counting\". Disabling any of\nthe three leaves what was already written in place - DeleteForgottenMemories and\nDeleteCallbackQueue are the explicit discards - so rows and bytes are reported whatever the\nsetting says, and enabled false beside a row count is a table nothing is adding to and nothing is\ntrimming."
    },
    "v1Bool": {
      "type": "string",
      "enum": [
        "UNSPECIFIED",
        "FALSE",
        "TRUE"
      ],
      "default": "UNSPECIFIED",
      "description": "Bool is a tri-state boolean (rather than proto3's inherent bool) so a caller can distinguish an\nexplicitly-set value from one left absent. It carries Memory.is_binary - where UNSPECIFIED and\nFALSE are treated identically by the server (both mean not-binary) and only TRUE marks a memory\nas binary - and the boolean list filters (GetMemoriesRequest's recalled/is_summary/is_binary),\nwhere the distinction matters rather more: a proto3 bool cannot say \"only the memories whose\nis_summary is false\", because an unset bool and an explicit false are the same byte on the wire.\n\n - UNSPECIFIED: not set; on Memory.is_binary the server treats this the same as FALSE, on a filter it applies no restriction\n - FALSE: explicitly false\n - TRUE: explicitly true"
    },
    "v1CallbackKind": {
      "type": "string",
      "enum": [
        "CALLBACK_KIND_UNSPECIFIED",
        "CALLBACK_KIND_MEMORY_FORGOTTEN",
        "CALLBACK_KIND_EVENT_FORGOTTEN",
        "CALLBACK_KIND_SLEEP_COMPLETED",
        "CALLBACK_KIND_MEMORIES_AT_RISK"
      ],
      "default": "CALLBACK_KIND_UNSPECIFIED",
      "description": "CallbackKind is what a queued delivery is about.\n\nMEMORIES_AT_RISK is the only one that is not about something that has already happened: it is\nraised at the top of a sleep cycle, from the same scan PreviewConsolidation runs, and describes\nwhat the store is about to forget. It is off by default because it costs that scan per cycle."
    },
    "v1ClearRequest": {
      "type": "object",
      "properties": {
        "manifestId": {
          "type": "string"
        }
      },
      "description": "Clear deletes exactly the records captured by the Export/Transfer run that produced the\nmanifest: memories are deleted only if neither created nor recalled since the snapshot, and\nevents only once they have no memories left. Manifests are held in memory - a restart discards\nthem."
    },
    "v1ClearResponse": {
      "type": "object",
      "properties": {
        "memoriesCleared": {
          "type": "integer",
          "format": "int32"
        },
        "eventsCleared": {
          "type": "integer",
          "format": "int32"
        }
      }
    },
    "v1CycleReport": {
      "type": "object",
      "properties": {
        "startedAt": {
          "type": "string",
          "format": "int64",
          "title": "UnixNano the cycle began"
        },
        "durationMs": {
          "type": "string",
          "format": "int64"
        },
        "memoriesConsolidated": {
          "type": "integer",
          "format": "int32",
          "description": "The two decay paths, reported separately because they answer different questions: memories\nconsolidated fell below the value threshold, memories evicted were above it but had to go\nanyway to bring the store back under its capacity target. A store evicting steadily while\nconsolidating nothing is configured wrongly, and only the split shows that."
        },
        "eventsConsolidated": {
          "type": "integer",
          "format": "int32"
        },
        "memoriesEvicted": {
          "type": "integer",
          "format": "int32"
        },
        "eventsEvicted": {
          "type": "integer",
          "format": "int32"
        },
        "bytesFreed": {
          "type": "string",
          "format": "int64",
          "title": "estimated, from the eviction pass"
        },
        "summarisationCandidates": {
          "type": "integer",
          "format": "int32",
          "title": "events the scan cached for GetSummarisationCandidates"
        },
        "success": {
          "type": "boolean"
        },
        "failure": {
          "type": "string",
          "title": "the reason when success is false; empty otherwise"
        },
        "trigger": {
          "type": "string",
          "description": "trigger names what STARTED this cycle: \"timer\" (the scheduled cycle), \"manual\" (the Sleep\nRPC) or \"wal\" (the write-ahead log grew past consolidation.walTriggerBytes). A caller that\njoined a cycle already in flight does not change it - this describes the cycle that ran, not\nthe call that observed it."
        },
        "externalBytesFreed": {
          "type": "string",
          "format": "int64",
          "description": "external_bytes_freed is bytes_freed's counterpart on the external axis (see\nMemory.external_bytes): what the cycle released in whatever system holds the payloads its\nmemories pointed at. Estimated from the eviction pass, exactly as bytes_freed is - the\nconsolidation passes stay on the covering index and count rows rather than bytes - so a cycle\nthat consolidated without evicting reports 0 here whatever it released. 0 always on a\ndeployment that stores no external sizes."
        },
        "stalled": {
          "type": "boolean",
          "description": "stalled reports that the two decay passes did not run: the cycle was held off rather than\nfinding nothing to forget. Without it a stalled cycle is indistinguishable from a quiet one -\nboth report zero consolidated and zero evicted - which is the reading an operator would draw\nexactly when the store has stopped doing the one thing it exists to do.\n\nToday the only thing that stalls a cycle is callbacks.backlogPolicy: stall with the outbound\ncallback queue over its caps, but the field is deliberately about the OUTCOME rather than that\ncause, so a later reason needs no second flag."
        },
        "stalledReason": {
          "type": "string",
          "description": "stalled_reason says why, in words meant to be shown rather than parsed. Empty unless stalled."
        }
      },
      "description": "CycleReport is what one sleep cycle did: the counts the two decay paths deleted, what it cost,\nand whether it succeeded. Counts only - no ids, no groups, no bodies - which is what keeps this\naggregate rather than an enumeration, and so readable by the reader tier.\n\nThe counts come from the passes themselves rather than from the metrics instruments they also\nfeed, so a deployment exporting no telemetry still gets them."
    },
    "v1DecayCurve": {
      "type": "object",
      "properties": {
        "significance": {
          "type": "number",
          "format": "double",
          "title": "the combined significance projected, echoed back"
        },
        "maxAgeDays": {
          "type": "number",
          "format": "double",
          "title": "the span actually projected over, whether requested or chosen by the server"
        },
        "crossingAgeDays": {
          "type": "number",
          "format": "double",
          "title": "the age at which the curve falls below deletion_threshold; -1 when it does not within the projected span"
        },
        "points": {
          "type": "array",
          "items": {
            "type": "object",
            "$ref": "#/definitions/v1DecayPoint"
          }
        }
      },
      "description": "DecayCurve is the projected decay of one significance value under the current configuration,\nsampled evenly from just after age zero to max_age_days. It is computed by the same code that\ndecides consolidation, so a plot of it cannot drift from what the service will actually do."
    },
    "v1DecayCurveRequest": {
      "type": "object",
      "properties": {
        "significance": {
          "type": "number",
          "format": "double",
          "title": "the combined significance to project; must be \u003e 0 - pass a memory's effective_significance to plot that memory's own curve"
        },
        "maxAgeDays": {
          "type": "number",
          "format": "double",
          "title": "the span to project over; \u003c= 0 asks the server to choose a span that shows the threshold crossing"
        },
        "points": {
          "type": "integer",
          "format": "int32",
          "title": "samples to return; \u003c= 0 selects the default (60), values above the cap (500) are clamped down to it"
        }
      },
      "description": "DecayCurveRequest asks for the curve the configured decay algorithm produces for one significance\nvalue, so a client can plot it without reimplementing the maths (which is what\ndocs/consolidation.md's value tables describe, and what the config wizard's preview approximates).\nLeaving the field unset asks for no curve."
    },
    "v1DecayPoint": {
      "type": "object",
      "properties": {
        "ageDays": {
          "type": "number",
          "format": "double"
        },
        "value": {
          "type": "number",
          "format": "double"
        }
      },
      "description": "DecayPoint is one sample of a decay curve: the value the configured algorithm gives an item of the\nrequested significance once its decay clock reads age_days."
    },
    "v1DeleteCallbackQueueRequest": {
      "type": "object",
      "properties": {
        "beforeTime": {
          "type": "string",
          "format": "int64",
          "title": "UnixNano; discards deliveries queued before this"
        },
        "all": {
          "type": "boolean",
          "title": "discard every delivery; ignored when before_time is set"
        }
      },
      "description": "DeleteCallbackQueueRequest empties the callback queue. Supply before_time to discard only what was\nqueued before it, or all to empty the queue entirely - one of the two is required, so an empty\nrequest can never silently discard every pending notification."
    },
    "v1DeleteCallbackQueueResponse": {
      "type": "object",
      "properties": {
        "deleted": {
          "type": "string",
          "format": "int64"
        }
      }
    },
    "v1DeleteCause": {
      "type": "string",
      "enum": [
        "DELETE_CAUSE_UNSPECIFIED",
        "DELETE_CAUSE_CONSOLIDATION",
        "DELETE_CAUSE_EVICTION",
        "DELETE_CAUSE_CLIENT",
        "DELETE_CAUSE_CLEAR",
        "DELETE_CAUSE_CASCADE",
        "DELETE_CAUSE_SUMMARY_REPLACE",
        "DELETE_CAUSE_PURGE"
      ],
      "default": "DELETE_CAUSE_UNSPECIFIED",
      "description": "DeleteCause is why the records a delivery describes were deleted. The two decay causes are the\nonly ones reported unless callbacks.allDeletions widens the feed."
    },
    "v1DeleteEventsByFilterRequest": {
      "type": "object",
      "properties": {
        "timeStartMin": {
          "type": "string",
          "format": "int64",
          "title": "UnixNano inclusive lower bound on time_start; 0 (the default) means no lower bound"
        },
        "timeStartMax": {
          "type": "string",
          "format": "int64",
          "title": "UnixNano inclusive upper bound on time_start; 0 (the default) means no upper bound"
        },
        "timeEndMin": {
          "type": "string",
          "format": "int64",
          "title": "UnixNano inclusive lower bound on time_end; 0 (the default) means no lower bound"
        },
        "timeEndMax": {
          "type": "string",
          "format": "int64",
          "title": "UnixNano inclusive upper bound on time_end; 0 (the default) means no upper bound. Open events store time_end = 0 and are excluded rather than matched - use ended for those"
        },
        "significanceMin": {
          "type": "integer",
          "format": "int32",
          "title": "inclusive lower bound on significance; 0 (the default) means no bound"
        },
        "significanceMax": {
          "type": "integer",
          "format": "int32",
          "title": "inclusive upper bound on significance; 0 (the default) means no bound"
        },
        "group": {
          "type": "string",
          "title": "restrict to events carrying this group label"
        },
        "significanceExtremum": {
          "$ref": "#/definitions/v1SignificanceExtremum",
          "title": "when set, ignore significance_min/significance_max and delete only the events tied at that significance value among the other filters"
        },
        "metadata": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "title": "restrict to events whose metadata carries every one of these \"key=value\" pairs (AND, exact match)"
        },
        "ended": {
          "$ref": "#/definitions/v1Bool",
          "title": "FALSE selects only events that have not ended, TRUE only those that have; UNSPECIFIED (default) applies no restriction"
        },
        "nameContains": {
          "type": "string",
          "title": "restrict to events whose name contains this substring, case-insensitively"
        },
        "maxDeletions": {
          "type": "string",
          "format": "int64",
          "title": "stop after deleting this many events; 0 (the default) means no bound. complete on the response reports which happened"
        },
        "deleteMemories": {
          "type": "boolean",
          "title": "delete each event's memories with it. Unset, they survive with their event_id cleared - the same choice DeleteEvent's memories flag makes, named unambiguously here because the listing's `memories` field means something else entirely"
        }
      },
      "description": "DeleteEventsByFilterRequest selects the events to delete. Every field means what the same field\nmeans on GetEventsRequest; order_by/order_dir/limit/offset, memories, memory_counts, links and\nlinked_to are absent for the reasons DeleteMemoriesByFilterRequest gives.\n\nAt least one selecting field must be set, delete_memories excepted - it says what to do with\nwhat is selected, not what to select, so a request carrying only delete_memories is still\nrefused."
    },
    "v1DeleteEventsByFilterResponse": {
      "type": "object",
      "properties": {
        "eventsDeleted": {
          "type": "string",
          "format": "int64"
        },
        "memoriesDeleted": {
          "type": "string",
          "format": "int64",
          "title": "memories deleted with their event; always 0 unless delete_memories was set"
        },
        "memoriesOrphaned": {
          "type": "string",
          "format": "int64",
          "title": "memories that outlived their event with event_id cleared; always 0 when delete_memories was set"
        },
        "complete": {
          "type": "boolean",
          "title": "true when the filter matched nothing more; false when max_deletions stopped the call"
        }
      },
      "description": "DeleteEventsByFilterResponse reports what actually went, on the same terms as\nDeleteMemoriesByFilterResponse."
    },
    "v1DeleteForgottenMemoriesRequest": {
      "type": "object",
      "properties": {
        "beforeTime": {
          "type": "string",
          "format": "int64",
          "title": "UnixNano; deletes records forgotten before this"
        },
        "all": {
          "type": "boolean",
          "title": "delete every record; ignored when before_time is set"
        }
      },
      "description": "DeleteForgottenMemoriesRequest empties the forgotten log. Supply before_time to delete only what\nwas forgotten before it, or all to empty the log entirely - one of the two is required, so an\nempty request can never silently discard the whole log."
    },
    "v1DeleteForgottenMemoriesResponse": {
      "type": "object",
      "properties": {
        "deleted": {
          "type": "string",
          "format": "int64"
        }
      }
    },
    "v1DeleteMemoriesByFilterRequest": {
      "type": "object",
      "properties": {
        "timestampMin": {
          "type": "string",
          "format": "int64",
          "title": "UnixNano inclusive lower bound on time_stamp; 0 (the default) means no lower bound"
        },
        "timestampMax": {
          "type": "string",
          "format": "int64",
          "title": "UnixNano inclusive upper bound on time_stamp; 0 (the default) means no upper bound"
        },
        "significanceMin": {
          "type": "integer",
          "format": "int32",
          "title": "inclusive lower bound on significance; 0 (the default) means no bound, NOT a literal significance-of-zero filter"
        },
        "significanceMax": {
          "type": "integer",
          "format": "int32",
          "title": "inclusive upper bound on significance; 0 (the default) means no bound, NOT a literal significance-of-zero filter"
        },
        "group": {
          "type": "string",
          "title": "restrict to memories carrying this group label - the field that makes offboarding a group one call"
        },
        "significanceExtremum": {
          "$ref": "#/definitions/v1SignificanceExtremum",
          "title": "when set, ignore significance_min/significance_max and delete only the memories tied at that significance value among the other filters; 0 (default) applies no extremum"
        },
        "metadata": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "title": "restrict to memories whose metadata carries every one of these \"key=value\" pairs (AND, exact match), exactly as GetMemoriesRequest.metadata"
        },
        "recalled": {
          "$ref": "#/definitions/v1Bool",
          "title": "FALSE selects only never-recalled memories, TRUE only those recalled at least once; UNSPECIFIED (default) applies no restriction"
        },
        "recallCountMin": {
          "type": "integer",
          "format": "int32",
          "title": "inclusive lower bound on recall_count; 0 (the default) means no bound"
        },
        "recallCountMax": {
          "type": "integer",
          "format": "int32",
          "title": "inclusive upper bound on recall_count; 0 (the default) means no bound"
        },
        "timeRecalledMin": {
          "type": "string",
          "format": "int64",
          "title": "UnixNano inclusive lower bound on time_recalled; 0 means no bound. Like GetMemoriesRequest's, both bounds ask only about memories that HAVE been recalled"
        },
        "timeRecalledMax": {
          "type": "string",
          "format": "int64",
          "title": "UnixNano inclusive upper bound on time_recalled; 0 means no bound. Never-recalled memories are excluded rather than matched - use recalled for those"
        },
        "isSummary": {
          "$ref": "#/definitions/v1Bool",
          "title": "TRUE selects only summary memories, FALSE only non-summaries; UNSPECIFIED (default) applies no restriction"
        },
        "isBinary": {
          "$ref": "#/definitions/v1Bool",
          "title": "TRUE selects only binary memories, FALSE only non-binary; UNSPECIFIED (default) applies no restriction"
        },
        "eventId": {
          "type": "string",
          "title": "restrict to one event's memories. Empty (the default) applies no restriction rather than matching the event-less; use has_event for those"
        },
        "hasEvent": {
          "$ref": "#/definitions/v1Bool",
          "title": "FALSE selects only memories associated with no event, TRUE only those associated with one; UNSPECIFIED (default) applies no restriction"
        },
        "maxDeletions": {
          "type": "string",
          "format": "int64",
          "title": "stop after deleting this many memories; 0 (the default) means no bound, so the call runs until the filter matches nothing. complete on the response reports which happened"
        },
        "deleteEmptyEvents": {
          "type": "boolean",
          "title": "also delete each event that the deletion left with no memories at all, exactly as Clear does. An event that still holds a memory is never touched"
        }
      },
      "description": "DeleteMemoriesByFilterRequest selects the memories to delete. Every field below means exactly\nwhat the same field means on GetMemoriesRequest, so the listing is the dry run for the deletion.\n\nWhat is deliberately NOT here, and why:\n\n  - order_by/order_dir/limit/offset. There is no page to order or skip; the call deletes\n    everything the predicate matches. max_deletions below bounds one call's work instead.\n  - links. There is no memory in the response to attach them to.\n  - linked_to. Deleting a memory removes its edges, so that predicate narrows as a consequence of\n    its own deletions: what the listing showed the caller and what the call would remove are not\n    the same set, and no amount of care on the caller's part could make them agree.\n\nAt least one selecting field must be set. A request with none is refused rather than read as\n\"everything\" - that operation exists, is called Purge, and is not something a defaulted field\nshould be able to reach."
    },
    "v1DeleteMemoriesByFilterResponse": {
      "type": "object",
      "properties": {
        "memoriesDeleted": {
          "type": "string",
          "format": "int64"
        },
        "eventsDeleted": {
          "type": "string",
          "format": "int64",
          "title": "events removed because the deletion left them empty; always 0 unless delete_empty_events was set"
        },
        "complete": {
          "type": "boolean",
          "title": "true when the filter matched nothing more; false when max_deletions stopped the call with matches still standing, so the same request can be sent again"
        }
      },
      "description": "DeleteMemoriesByFilterResponse reports what actually went. The counts are what this call\ndeleted, not what the filter matched: a concurrent write can add a matching memory while the\ncall runs, and it will be deleted too if the call has not finished."
    },
    "v1DeleteMemoriesRequest": {
      "type": "object",
      "properties": {
        "ids": {
          "type": "array",
          "items": {
            "type": "string"
          }
        }
      }
    },
    "v1Event": {
      "type": "object",
      "properties": {
        "id": {
          "type": "string",
          "title": "auto-generated UUID if omitted on create; max 128 characters"
        },
        "timeStart": {
          "type": "string",
          "format": "int64",
          "title": "UnixNano; defaults to the current time when unset (0) on create; required (\u003e 0) once defaulted"
        },
        "timeEnd": {
          "type": "string",
          "format": "int64",
          "title": "UnixNano; 0 means the event has not ended yet (see EndEvent); when set, must not be before time_start"
        },
        "significance": {
          "type": "integer",
          "format": "int32",
          "title": "0 = unranked (rank it later via UpdateEventSignificance/placement); must be \u003e= 0; higher ranks more significant"
        },
        "name": {
          "type": "string",
          "title": "required (non-empty) on create; max 256 characters"
        },
        "description": {
          "type": "string",
          "title": "optional; max 1024 characters"
        },
        "memoriesConsolidated": {
          "type": "boolean",
          "title": "read-only: true once a sleep cycle has deleted (consolidated) some of this event's memories; ignored on write"
        },
        "linkSignificance": {
          "type": "string",
          "format": "int64",
          "title": "read-only: sum of this event's links' significance values, inbound and outbound, calculated by the server; ignored on write"
        },
        "links": {
          "type": "array",
          "items": {
            "type": "object",
            "$ref": "#/definitions/v1Link"
          },
          "title": "links this event declares to other events, weighted into its memories' value (see consolidation.linkSignificanceWeight). Outbound only, like Memory.links - ask GetEventLinks for both directions"
        },
        "memories": {
          "type": "array",
          "items": {
            "type": "object",
            "$ref": "#/definitions/v1Memory"
          },
          "title": "populated on read only when requested (GetEventById/GetEvents); write-only input on StoreEvent, stored via the same rules as StoreMemory"
        },
        "group": {
          "type": "string",
          "title": "optional freeform grouping/context label (system, subsystem, owner, ...); max 128 characters"
        },
        "placement": {
          "$ref": "#/definitions/v1SignificancePlacement",
          "title": "write-only: rank relative to existing values (see SignificancePlacement)"
        },
        "metadata": {
          "type": "object",
          "additionalProperties": {
            "type": "string"
          },
          "title": "optional multi-dimensional classification, exactly as Memory.metadata: same key charset, same 32-key/512-byte-value/4096-byte bounds, same replace-wholesale semantics on update, same opacity to the server"
        },
        "clearMetadata": {
          "type": "boolean",
          "title": "write-only: on UpdateEvent, remove all metadata (see Memory.clear_metadata for why the empty map cannot say it)"
        },
        "clearGroup": {
          "type": "boolean",
          "title": "write-only: on UpdateEvent, reset group to empty (see Memory.clear_group)"
        },
        "memoryCount": {
          "type": "integer",
          "format": "int32",
          "title": "read-only: memories currently attached to this event, populated only when asked for (GetEventsRequest.memory_counts, GetEventByIdRequest.memory_counts - see there for why it is opt-in); 0 otherwise, which is indistinguishable from an event holding none, so ask for the count if you need to tell them apart. Counted within the caller's group scope, exactly as the nested memories are filtered. Ignored on write"
        }
      },
      "description": "Event is a named time span with its own significance, optionally carrying links to\nother events and/or nested memories. memories/links are populated on read only when\nrequested (GetEventById, GetEvents); on StoreEvent nested memories are write-only input, each\nstored via the same rules as StoreMemory."
    },
    "v1ExplainConsolidationRequest": {
      "type": "object",
      "properties": {
        "memoryIds": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "title": "the memories to value; at most 200 per call"
        },
        "curve": {
          "$ref": "#/definitions/v1DecayCurveRequest",
          "title": "optional projected curve for the current configuration"
        }
      },
      "description": "ExplainConsolidationRequest asks where the named memories stand against the consolidation rules,\nand optionally for the decay curve of the current configuration. Both parts are optional: ids\nwithout a curve values just those memories, a curve without ids reports the configuration and the\ncurrent capacity pressure alone. Ids that do not exist are simply absent from the response."
    },
    "v1ExplainConsolidationResponse": {
      "type": "object",
      "properties": {
        "capacityPressure": {
          "type": "number",
          "format": "double",
          "title": "multiplier applied to the deletion threshold by how full the store is (1.0 = no effect)"
        },
        "deletionThreshold": {
          "type": "number",
          "format": "double",
          "title": "the scaled threshold actually applied: deletionThreshold * capacity_pressure"
        },
        "usedBytes": {
          "type": "string",
          "format": "int64",
          "title": "the store's current used bytes"
        },
        "capacityBytes": {
          "type": "string",
          "format": "int64",
          "title": "consolidation.capacityBytes; 0 when no byte capacity is configured"
        },
        "memoryCount": {
          "type": "integer",
          "format": "int32",
          "title": "memories currently stored - the other axis capacity pressure rides on"
        },
        "capacityMemories": {
          "type": "integer",
          "format": "int32",
          "title": "consolidation.capacityMemories; 0 when no row capacity is configured"
        },
        "method": {
          "type": "integer",
          "format": "int32",
          "description": "The configured decay policy, so a client can label what it is showing without a second source\nof truth for it.\n\nconsolidation.method (1-6; see docs/consolidation.md)"
        },
        "aggressiveness": {
          "type": "number",
          "format": "double"
        },
        "unitsOfAgeInDays": {
          "type": "number",
          "format": "double",
          "title": "days per unit of age fed to the decay algorithm"
        },
        "minimumAgeInDays": {
          "type": "integer",
          "format": "int32"
        },
        "minimumRetentionInDays": {
          "type": "integer",
          "format": "int32"
        },
        "valuations": {
          "type": "array",
          "items": {
            "type": "object",
            "$ref": "#/definitions/v1MemoryValuation"
          }
        },
        "curve": {
          "$ref": "#/definitions/v1DecayCurve",
          "title": "absent when none was requested"
        },
        "externalBytes": {
          "type": "string",
          "format": "int64",
          "description": "The third pressure axis (see Memory.external_bytes). Reported beside the other two because\ncapacity_pressure above is the greater of all three utilisations, so a client showing the\npressure without this cannot explain a reading the store's own size does not account for.\nBoth are 0 when no external capacity is configured, in which case the axis contributes nothing."
        },
        "capacityExternalBytes": {
          "type": "string",
          "format": "int64",
          "title": "consolidation.capacityExternalBytes"
        }
      },
      "description": "ExplainConsolidationResponse carries the decision inputs, one valuation per memory found, and the\nrequested curve. The inputs are a snapshot: capacity pressure and used bytes are re-read\nperiodically rather than per call, so they may lag a cycle that has just run - by design, since\nthis is asked far more often than a preview and both readings cost a scan."
    },
    "v1ExportRequest": {
      "type": "object",
      "properties": {
        "clear": {
          "type": "boolean"
        }
      },
      "description": "Export snapshots every event and memory into an archive object in S3 and records a manifest of\nexactly what was captured; with clear set, the captured records are deleted after a successful\nupload (writes and recalls landing mid-run survive, exactly as Clear does)."
    },
    "v1ExportResponse": {
      "type": "object",
      "properties": {
        "manifestId": {
          "type": "string"
        },
        "objectKey": {
          "type": "string"
        },
        "eventsExported": {
          "type": "integer",
          "format": "int32"
        },
        "memoriesExported": {
          "type": "integer",
          "format": "int32"
        },
        "memoriesCleared": {
          "type": "integer",
          "format": "int32"
        },
        "eventsCleared": {
          "type": "integer",
          "format": "int32"
        }
      }
    },
    "v1ForgetCandidate": {
      "type": "object",
      "properties": {
        "id": {
          "type": "string"
        },
        "eventId": {
          "type": "string",
          "title": "empty for a memory with no event"
        },
        "group": {
          "type": "string"
        },
        "significance": {
          "type": "integer",
          "format": "int32",
          "title": "the memory's stored significance, as ranked"
        },
        "value": {
          "type": "number",
          "format": "double",
          "title": "the computed decayed value that decided it (see docs/consolidation.md)"
        },
        "threshold": {
          "type": "number",
          "format": "double",
          "title": "the capacity-pressure-scaled deletion threshold value was compared against"
        },
        "bodyBytes": {
          "type": "string",
          "format": "int64",
          "title": "stored size of the body, after compression - the same measure capacity accounting uses"
        },
        "rule": {
          "$ref": "#/definitions/v1ForgetRule"
        },
        "timeStamp": {
          "type": "string",
          "format": "int64",
          "title": "UnixNano"
        },
        "timeRecalled": {
          "type": "string",
          "format": "int64",
          "title": "UnixNano of the most recent recall; 0 if never recalled"
        },
        "recallCount": {
          "type": "integer",
          "format": "int32"
        },
        "externalBytes": {
          "type": "string",
          "format": "int64",
          "title": "Memory.external_bytes: the payload elsewhere that forgetting this memory releases; 0 for a memory pointing at nothing"
        }
      },
      "description": "ForgetCandidate is one memory a cycle would delete, with the numbers behind the decision. value\nand threshold are the two sides of the comparison that decided it: a CONSOLIDATION candidate is\none whose value fell below the threshold. Bodies are never included - a preview reports what\nwould be lost, it is not a way to read the store."
    },
    "v1ForgetRule": {
      "type": "string",
      "enum": [
        "FORGET_RULE_UNSPECIFIED",
        "FORGET_RULE_CONSOLIDATION",
        "FORGET_RULE_EVICTION"
      ],
      "default": "FORGET_RULE_UNSPECIFIED",
      "description": "ForgetRule names why a memory appears in a consolidation preview - which of the two independent\ndeletion paths selected it. The distinction matters when tuning: a memory forgotten by\nCONSOLIDATION decayed below the threshold and is answered by the decay settings\n(aggressiveness, deletionThreshold, unitsOfAgeInDays), while one forgotten by EVICTION was\nstill valuable enough to keep and went only because the store is over its byte capacity - which\nis answered by capacityBytes, or by storing less.\n\n - FORGET_RULE_CONSOLIDATION: decayed value fell below the capacity-pressure-scaled deletion threshold\n - FORGET_RULE_EVICTION: still above the threshold, but evicted to bring the store back under its byte capacity"
    },
    "v1ForgottenMemory": {
      "type": "object",
      "properties": {
        "seq": {
          "type": "string",
          "format": "int64",
          "title": "the log's own monotonic key; pass the lowest seen back as after_seq to page"
        },
        "id": {
          "type": "string",
          "title": "the forgotten memory's id"
        },
        "eventId": {
          "type": "string",
          "title": "empty for a memory with no event"
        },
        "group": {
          "type": "string"
        },
        "significance": {
          "type": "integer",
          "format": "int32",
          "title": "the memory's significance, as ranked when it went"
        },
        "value": {
          "type": "number",
          "format": "double",
          "title": "its computed decayed value at that moment (see docs/consolidation.md)"
        },
        "threshold": {
          "type": "number",
          "format": "double",
          "title": "the capacity-pressure-scaled deletion threshold then in force"
        },
        "bodyBytes": {
          "type": "string",
          "format": "int64",
          "title": "stored size of the body it held, after compression"
        },
        "rule": {
          "$ref": "#/definitions/v1ForgetRule",
          "title": "which path took it"
        },
        "timeStamp": {
          "type": "string",
          "format": "int64",
          "title": "UnixNano the memory was created"
        },
        "timeRecalled": {
          "type": "string",
          "format": "int64",
          "title": "UnixNano of its last recall; 0 if never recalled"
        },
        "recallCount": {
          "type": "integer",
          "format": "int32"
        },
        "forgottenAt": {
          "type": "string",
          "format": "int64",
          "title": "UnixNano the cycle deleted it"
        }
      },
      "description": "ForgottenMemory is one record from the forgotten log: a memory the sleep cycle deleted, and the\ndecision that took it. value and threshold are the two sides of the comparison as they stood at\nthat moment - the threshold moves with capacity pressure, so recording it is what keeps value\ninterpretable afterwards.\n\nThere is no body, and there never will be: this says a memory was forgotten, not what it said."
    },
    "v1GeneralResponse": {
      "type": "object",
      "properties": {
        "ok": {
          "type": "boolean"
        }
      }
    },
    "v1GetCallbackQueueResponse": {
      "type": "object",
      "properties": {
        "deliveries": {
          "type": "array",
          "items": {
            "type": "object",
            "$ref": "#/definitions/v1QueuedCallback"
          }
        },
        "depth": {
          "type": "string",
          "format": "int64",
          "title": "every delivery waiting, not just this page"
        },
        "enabled": {
          "type": "boolean"
        },
        "oldestQueuedAt": {
          "type": "string",
          "format": "int64",
          "title": "UnixNano; 0 when the queue is empty"
        },
        "nextSeq": {
          "type": "string",
          "format": "int64",
          "title": "pass as after_seq for the next page; 0 when the page is the last"
        }
      },
      "description": "GetCallbackQueueResponse carries one page of the queue.\n\nenabled reports whether the service is currently recording deliveries. It matters for the same\nreason the forgotten log's does: an empty queue is ambiguous without it - everything has been\ndelivered, or nothing is being queued."
    },
    "v1GetConsolidationStatusResponse": {
      "type": "object",
      "properties": {
        "consolidationEnabled": {
          "type": "boolean"
        },
        "periodSeconds": {
          "type": "string",
          "format": "int64",
          "description": "period_seconds is sleep.periodSeconds. Zero or negative means no timed cycle at all - a\nsupported mode, for an instance driven only by the Sleep RPC or the WAL trigger - and\nnext_sleep_at is then 0 rather than a countdown to something that will never fire."
        },
        "nextSleepAt": {
          "type": "string",
          "format": "int64",
          "title": "UnixNano; 0 when no timed cycle is scheduled"
        },
        "walTriggerEnabled": {
          "type": "boolean",
          "description": "wal_trigger_enabled reports consolidation.walTriggerBytes \u003e 0, i.e. that a cycle may run\nout of schedule when the write-ahead log grows. next_sleep_at deliberately does NOT predict\nthat: the WAL trigger does not reset the timer, so the timed cycle is still due when it says,\nand a client showing a countdown should say \"or sooner\" rather than imply the schedule is\nthe only thing that can fire."
        },
        "sleepInProgress": {
          "type": "boolean"
        },
        "snapshotTtlSeconds": {
          "type": "string",
          "format": "int64",
          "description": "snapshot_ttl_seconds is how long ExplainConsolidation caches the inputs it reports\n(capacity pressure, used bytes, memory count). A polling client should pace its calls to that\nRPC by this rather than by a guess: the snapshot costs a full scan on the server drivers and\nis cached precisely so a console page does not repeat it, so polling faster buys nothing and\ndefeats the cache under concurrent viewers."
        },
        "lastCycle": {
          "$ref": "#/definitions/v1CycleReport",
          "description": "last_cycle is absent until a cycle has run in THIS process - it is in-memory, not persisted,\nso a restart clears it. That is deliberate: it reports what this instance did, and a figure\nsurviving the process that produced it would describe a cycle run under configuration that\nmay since have changed."
        },
        "ancillary": {
          "$ref": "#/definitions/v1AncillaryStorage",
          "description": "ancillary is what the capacity target deliberately does NOT count: the forgotten log, the\nsearch outbox and the callback queue. Absent until a cycle in THIS process has measured it,\nand on a replica always - it is measured by the sleep cycle rather than per request, since a\ncount of the callback queue is a scan of up to callbacks.maxRows rows and this is an RPC a\nconsole polls."
        }
      },
      "description": "GetConsolidationStatusResponse reports the sleep cycle's schedule and its last result.\n\nEverything below consolidation_enabled is zero on a read/write replica, which runs no cycle of\nits own: its store is consolidated by whichever instance holds the single-consolidator lock,\nunder that instance's configuration."
    },
    "v1GetEventResponse": {
      "type": "object",
      "properties": {
        "event": {
          "$ref": "#/definitions/v1Event"
        }
      }
    },
    "v1GetEventsResponse": {
      "type": "object",
      "properties": {
        "events": {
          "type": "array",
          "items": {
            "type": "object",
            "$ref": "#/definitions/v1Event"
          }
        },
        "totalCount": {
          "type": "integer",
          "format": "int32",
          "title": "events matching the filter, ignoring limit/offset — for pagination"
        }
      }
    },
    "v1GetForgottenMemoriesResponse": {
      "type": "object",
      "properties": {
        "memories": {
          "type": "array",
          "items": {
            "type": "object",
            "$ref": "#/definitions/v1ForgottenMemory"
          }
        },
        "total": {
          "type": "string",
          "format": "int64"
        },
        "enabled": {
          "type": "boolean"
        },
        "nextSeq": {
          "type": "string",
          "format": "int64",
          "title": "pass as after_seq for the next page; 0 when the page is the last"
        }
      },
      "description": "GetForgottenMemoriesResponse carries one page of the log. total counts every record the log\nholds within the caller's scope, not the filtered subset, so a client can show how much history\nexists behind the page it is looking at.\n\nenabled reports whether the service is currently recording. It matters because an empty page is\nambiguous without it: nothing has been forgotten, or nothing is being written down."
    },
    "v1GetLinksResponse": {
      "type": "object",
      "properties": {
        "links": {
          "type": "array",
          "items": {
            "type": "object",
            "$ref": "#/definitions/v1LinkEdge"
          }
        },
        "linkSignificance": {
          "type": "string",
          "format": "int64",
          "title": "the queried item's total link significance, both directions - the figure the decay maths damps and weights"
        }
      }
    },
    "v1GetMemoriesResponse": {
      "type": "object",
      "properties": {
        "memories": {
          "type": "array",
          "items": {
            "type": "object",
            "$ref": "#/definitions/v1Memory"
          }
        },
        "totalCount": {
          "type": "integer",
          "format": "int32",
          "title": "memories matching the filter, ignoring limit/offset — for pagination"
        }
      }
    },
    "v1GetSignificanceLevelsResponse": {
      "type": "object",
      "properties": {
        "significances": {
          "type": "array",
          "items": {
            "type": "integer",
            "format": "int32"
          },
          "title": "the distinct significance values in use, ascending. Two ADJACENT values have no room between them - that is what SignificancePlacement exists to open, and what makes this list the set of anchors a placement can name"
        },
        "totalCount": {
          "type": "integer",
          "format": "int32",
          "title": "values matching the bounds, ignoring limit/offset - for pagination"
        }
      },
      "description": "GetSignificanceLevelsResponse lists the significance values in use, ascending."
    },
    "v1GetSummarisationCandidatesResponse": {
      "type": "object",
      "properties": {
        "candidates": {
          "type": "array",
          "items": {
            "type": "object",
            "$ref": "#/definitions/v1SummarisationCandidate"
          }
        },
        "scanEnabled": {
          "type": "boolean",
          "description": "scan_enabled reports whether this instance runs the candidate scan at all: it needs\nconsolidation.summarisationMinMemories \u003e 0 AND consolidation.enabled, since the scan is part of\nthe sleep cycle and a replica runs none. It exists because an empty candidates list is\nambiguous in the way that matters, exactly as WhoAmI.groups is - \"nothing is due yet\" and \"this\ninstance will never offer you anything\" are opposite answers, and only one of them is worth\nwaiting on. A client presenting an empty list must branch on this rather than on the list."
        }
      }
    },
    "v1GetTopologyResponse": {
      "type": "object",
      "properties": {
        "nodes": {
          "type": "array",
          "items": {
            "type": "object",
            "$ref": "#/definitions/v1TopologyNode"
          }
        },
        "edges": {
          "type": "array",
          "items": {
            "type": "object",
            "$ref": "#/definitions/v1TopologyEdge"
          }
        },
        "probeIntervalSeconds": {
          "type": "string",
          "format": "int64",
          "description": "probe_interval_seconds is how often the background prober refreshes the statuses above. A\npolling client should pace itself by this rather than by a guess, for the same reason\nGetConsolidationStatus reports snapshot_ttl_seconds: polling faster returns the same snapshot\nand only costs the server the assembly."
        },
        "generatedAt": {
          "type": "string",
          "format": "int64",
          "title": "UnixNano; when this response was assembled, not when it was probed"
        },
        "warnings": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "description": "warnings are conditions about the deployment AS A WHOLE that no single node can express,\nwritten for an operator to read verbatim. There is exactly one source of them today: the\ninstance registry's count of how many instances are running consolidation, where both zero\n(nothing is forgetting or evicting, and the store simply grows) and two or more (the\nsingle-consolidator lock has been circumvented, or two tiers are pointed at different\ndatabases) are faults with no node to attach themselves to - the zero case in particular is\nthe absence of a node, which is precisely why it has gone unreported until now.\n\nEmpty is the normal case. A client should show these prominently and unconditionally; they\nare not a status, and nothing else in the response repeats them."
        }
      },
      "description": "GetTopologyResponse is the deployment as this instance understands it: a flat node list plus the\nedges between them, rather than a tree. The graph is not a tree - two instances share one store -\nand a flat shape means a new component type is a new TopologyNodeKind rather than a new field."
    },
    "v1ImportBatchRequest": {
      "type": "object",
      "properties": {
        "events": {
          "type": "array",
          "items": {
            "type": "object",
            "$ref": "#/definitions/v1Event"
          }
        },
        "memories": {
          "type": "array",
          "items": {
            "type": "object",
            "$ref": "#/definitions/v1Memory"
          }
        }
      },
      "description": "ImportBatchRequest carries full-state rows (original timestamps, recall state, significance,\ngroup, event links) to be upserted by id into the store as-is - a data migration, not a fresh\nwrite. It is the ingest half of Transfer and is re-sendable: re-importing the same rows is\nidempotent."
    },
    "v1ImportBatchResponse": {
      "type": "object",
      "properties": {
        "eventsImported": {
          "type": "integer",
          "format": "int32"
        },
        "memoriesImported": {
          "type": "integer",
          "format": "int32"
        }
      }
    },
    "v1ImportRequest": {
      "type": "object",
      "properties": {
        "objectKey": {
          "type": "string"
        }
      }
    },
    "v1ImportResponse": {
      "type": "object",
      "properties": {
        "eventsImported": {
          "type": "integer",
          "format": "int32"
        },
        "memoriesImported": {
          "type": "integer",
          "format": "int32"
        }
      }
    },
    "v1Link": {
      "type": "object",
      "properties": {
        "id": {
          "type": "string",
          "title": "the linked memory's or event's id; must name an existing item of the same kind"
        },
        "significance": {
          "type": "integer",
          "format": "int32",
          "title": "this link's weight; 0..1000000, must not be negative - not part of the significance registry, never ranked"
        }
      },
      "description": "Link is a directed, significance-carrying association between two memories or between two events.\nOne Link message describes one edge from the item carrying it to the item it names.\n\nThe significance is weighted into the decay maths for BOTH ends, with diminishing returns - the\ncontribution is consolidation.linkSignificanceWeight * log1p(sum of an item's link significances),\nso a well-connected memory decays more slowly without any number of links ever making it\nunforgettable. Unlike the event relationships this replaces, both ends must exist: a link is\ndeleted automatically when either end is forgotten, so it can never dangle."
    },
    "v1LinkDirection": {
      "type": "string",
      "enum": [
        "LINK_DIRECTION_UNSPECIFIED",
        "LINK_DIRECTION_BOTH",
        "LINK_DIRECTION_OUTBOUND",
        "LINK_DIRECTION_INBOUND"
      ],
      "default": "LINK_DIRECTION_UNSPECIFIED",
      "description": "LinkDirection narrows which of an item's links a read returns. The graph is stored directed, so\nthe two directions are distinguishable, but value is symmetric - both ends of a link gain its\nsignificance - which is why BOTH is the default rather than OUTBOUND.\n\n - LINK_DIRECTION_UNSPECIFIED: treated as BOTH\n - LINK_DIRECTION_BOTH: links in either direction\n - LINK_DIRECTION_OUTBOUND: only links this item declared, pointing away from it\n - LINK_DIRECTION_INBOUND: only links other items declared, pointing at it"
    },
    "v1LinkEdge": {
      "type": "object",
      "properties": {
        "id": {
          "type": "string",
          "title": "the far end's id - the other memory or event"
        },
        "significance": {
          "type": "integer",
          "format": "int32",
          "title": "this link's weight"
        },
        "direction": {
          "$ref": "#/definitions/v1LinkDirection",
          "title": "OUTBOUND when the queried item declared this link, INBOUND when the far end did"
        },
        "created": {
          "type": "string",
          "format": "int64",
          "title": "UnixNano the link was first created"
        }
      },
      "description": "LinkEdge is one stored link as read back: the pair it connects, its significance, and which way\nit points relative to the item that was asked about. Link (the write form) names only the far\nend, because the near end is the item carrying it; a read can return both directions at once and\nso has to say which is which."
    },
    "v1Memory": {
      "type": "object",
      "properties": {
        "id": {
          "type": "string",
          "title": "auto-generated UUID if omitted on create; max 128 characters"
        },
        "timeStamp": {
          "type": "string",
          "format": "int64",
          "title": "UnixNano; defaults to the current time when unset (0) on create; required (\u003e 0) once defaulted"
        },
        "significance": {
          "type": "integer",
          "format": "int32",
          "title": "0 = unranked (rank it later via UpdateMemory/placement); must be \u003e= 0; higher ranks more significant. On UpdateMemory, 0 with no placement leaves the existing significance unchanged - it cannot reset a memory to unranked"
        },
        "eventId": {
          "type": "string",
          "title": "optional: leave unset to store the memory unassociated with any event"
        },
        "body": {
          "type": "string",
          "title": "required (non-empty) on create; a proto3 string, so always valid UTF-8 on the wire - encode true binary payloads (e.g. base64) before setting is_binary; optionally capped by memory.limit.sizeBytes (0 = unlimited)"
        },
        "isBinary": {
          "$ref": "#/definitions/v1Bool",
          "title": "set once at creation; not updatable via UpdateMemory. TRUE bodies are opaque and never indexed for content search (see SearchMemories)"
        },
        "timeRecalled": {
          "type": "string",
          "format": "int64",
          "title": "read-only: UnixNano of the most recent recall (RecallMemories, or SearchMemories with reinforce); 0 if never recalled; ignored on write"
        },
        "recallCount": {
          "type": "integer",
          "format": "int32",
          "title": "read-only: number of times the memory has been recalled; ignored on write"
        },
        "isSummary": {
          "type": "boolean",
          "title": "read-only: true for the summary memory created by ReplaceMemoriesWithSummary; not updatable via UpdateMemory"
        },
        "group": {
          "type": "string",
          "title": "optional freeform grouping/context label (system, subsystem, owner, ...); max 128 characters"
        },
        "placement": {
          "$ref": "#/definitions/v1SignificancePlacement",
          "title": "write-only: rank relative to existing values (see SignificancePlacement)"
        },
        "links": {
          "type": "array",
          "items": {
            "type": "object",
            "$ref": "#/definitions/v1Link"
          },
          "title": "associative links this memory declares, weighted into its value (see consolidation.linkSignificanceWeight). Outbound only - Link is a directed edge from the item carrying it, so this lists exactly the edges this memory owns and an export/import round trip reproduces them once each; ask GetMemoryLinks for both directions. Write-only on StoreMemory (targets must already exist) and populated on read only when requested; use LinkMemories/UnlinkMemories to edit them incrementally"
        },
        "linkSignificance": {
          "type": "string",
          "format": "int64",
          "title": "read-only: sum of this memory's links' significance values, inbound and outbound, calculated by the server; ignored on write"
        },
        "metadata": {
          "type": "object",
          "additionalProperties": {
            "type": "string"
          },
          "title": "optional multi-dimensional classification (source=slack, project=..., author=...), where group is the single freeform label. Keys match [A-Za-z0-9][A-Za-z0-9._:/-]{0,63}; values are any UTF-8 up to 512 bytes; at most 32 keys and 4096 bytes of serialised JSON in total, and those bytes count toward memory.limit.sizeBytes alongside the body. On UpdateMemory a non-empty map REPLACES the stored map wholesale - there is no per-key merge, and an absent or empty map leaves it untouched (use clear_metadata to remove it). Opaque to the server: filterable via GetMemories/SearchMemories, never interpreted, and never emitted as a metric, span, or log attribute"
        },
        "clearMetadata": {
          "type": "boolean",
          "title": "write-only: on UpdateMemory, remove all metadata. Exists because the map's own emptiness cannot say it - an absent map and an explicitly empty one are the same on the wire, and every other updatable field reads unset as \"leave unchanged\". Ignored on StoreMemory; takes precedence over any metadata supplied alongside it"
        },
        "clearGroup": {
          "type": "boolean",
          "title": "write-only: on UpdateMemory, reset group to empty, for the same reason clear_metadata exists - an empty group string otherwise means \"leave unchanged\". Ignored on StoreMemory; takes precedence over any group supplied alongside it"
        },
        "externalBytes": {
          "type": "string",
          "format": "int64",
          "description": "external_bytes is the size of a payload this memory POINTS AT rather than holds: the trace,\nblob or document that lives in another system and that this memory's forgetting is meant to\ngovern. It is client-supplied and never interpreted - the service does not fetch, verify or\neven know the address of whatever it measures - and it exists for one reason: capacity\npressure is the control this store has that an age-based expiry does not, and pressure\ncomputed over a couple of hundred bytes of pointer while the payload behind it is forty\nkilobytes is regulating a quantity with no relationship to the resource it exists to protect.\n\nIt is a THIRD capacity axis (consolidation.capacityExternalBytes), never folded into\nused_bytes: used_bytes is this store's own footprint and protects its own disk, and\nconflating the two would let external volume evict memories to reclaim space it is not using.\nEviction counts it toward what a pass has reclaimed, but the ordering is unchanged - least\nvaluable still goes first, not worst value density.\n\nMust be \u003e= 0. On UpdateMemory 0 leaves the stored value unchanged, exactly as significance\ndoes, so a partial update of other fields cannot silently zero it; a memory whose payload is\ngone is a memory to delete rather than one to set back to zero."
        }
      },
      "description": "Memory is a blob, opaque to the server, with a significance and timestamp, optionally linked to\nan event. Recalling it (RecallMemories) reinforces it: the decay clock resets and its effective\nsignificance rises. is_binary controls whether the body is indexed for content search (see\nSearchMemories) - a binary body is never indexed."
    },
    "v1MemoryValuation": {
      "type": "object",
      "properties": {
        "id": {
          "type": "string"
        },
        "eventId": {
          "type": "string",
          "title": "empty for a memory with no event (or one whose event no longer exists)"
        },
        "significance": {
          "type": "integer",
          "format": "int32",
          "title": "the memory's own stored significance, as ranked"
        },
        "effectiveSignificance": {
          "type": "number",
          "format": "double",
          "title": "what the decay actually acts on: the memory's own significance plus its event's, the damped link contributions below, and the weighted recall count"
        },
        "value": {
          "type": "number",
          "format": "double",
          "title": "the computed decayed value (see docs/consolidation.md)"
        },
        "threshold": {
          "type": "number",
          "format": "double",
          "title": "the capacity-pressure-scaled deletion threshold value is compared against"
        },
        "ageDays": {
          "type": "number",
          "format": "double",
          "title": "days since the memory's decay clock last reset - its creation, or its most recent recall"
        },
        "recallCount": {
          "type": "integer",
          "format": "int32"
        },
        "timeRecalled": {
          "type": "string",
          "format": "int64",
          "title": "UnixNano of the most recent recall; 0 if never recalled"
        },
        "wouldConsolidate": {
          "type": "boolean",
          "title": "a cycle running now would forget this memory"
        },
        "retained": {
          "type": "boolean",
          "title": "held by consolidation.minimumRetentionInDays, so neither consolidation nor capacity eviction may take it"
        },
        "belowMinimumAge": {
          "type": "boolean",
          "title": "younger than consolidation.minimumAgeInDays, so value-based consolidation is deferred whatever the value says"
        },
        "daysUntilForgotten": {
          "type": "number",
          "format": "double",
          "title": "projected days until the memory would be consolidated, holding today's threshold and pressure and assuming no further recall; 0 when it is already due, -1 when it is not due within the projected horizon"
        },
        "linkSignificance": {
          "type": "string",
          "format": "int64",
          "title": "the memory's own summed link significance, both directions, before damping"
        },
        "linkContribution": {
          "type": "number",
          "format": "double",
          "title": "what those links add to effective_significance: linkSignificanceWeight * log1p(link_significance)"
        },
        "eventLinkSignificance": {
          "type": "string",
          "format": "int64",
          "title": "the memory's event's summed link significance, both directions, before damping; 0 for a memory with no event"
        },
        "eventLinkContribution": {
          "type": "number",
          "format": "double",
          "title": "what the event's links add to effective_significance, damped the same way; 0 for a memory with no event"
        }
      },
      "description": "MemoryValuation is where one memory stands against the consolidation rules right now: the value\nthe decay algorithm gives it, the threshold that value is measured against, and the flags that\noverride the comparison either way. Bodies are never included - this explains a memory, it is not\na way to read one."
    },
    "v1MergeEventsRequest": {
      "type": "object",
      "properties": {
        "mergeTo": {
          "type": "string",
          "title": "the surviving event; must already exist"
        },
        "mergeFrom": {
          "type": "string",
          "title": "the event whose memories are moved to merge_to; need not exist - an absent one simply matches no memories"
        }
      }
    },
    "v1PreviewConsolidationResponse": {
      "type": "object",
      "properties": {
        "memoriesConsolidated": {
          "type": "integer",
          "format": "int32",
          "title": "memories whose value fell below the threshold"
        },
        "memoriesEvicted": {
          "type": "integer",
          "format": "int32",
          "title": "memories capacity eviction would additionally delete"
        },
        "eventsDeleted": {
          "type": "integer",
          "format": "int32",
          "title": "events that would go: those left with no memories, plus empty events past their own threshold"
        },
        "bytesFreed": {
          "type": "string",
          "format": "int64",
          "title": "estimated bytes reclaimed by both paths together, on the same basis as used_bytes"
        },
        "memoriesRetained": {
          "type": "integer",
          "format": "int32",
          "description": "memories_retained counts the memories that would otherwise have been forgotten but are held\nby consolidation.minimumRetentionInDays, with retained_bytes their stored size. Watch this\nagainst capacity_bytes: retention overrides the capacity target, so once retained_bytes\napproaches capacity_bytes the store cannot be brought back under its target however hard\neviction runs. They are counted, never listed - on a healthy store almost everything is\nretained, so listing them would return the store."
        },
        "retainedBytes": {
          "type": "string",
          "format": "int64"
        },
        "capacityPressure": {
          "type": "number",
          "format": "double",
          "description": "The decision inputs as they stood for this preview, so the numbers above can be read against\nthe configuration that produced them.\n\nmultiplier applied to the deletion threshold by how full the store is (1.0 = no effect)"
        },
        "deletionThreshold": {
          "type": "number",
          "format": "double",
          "title": "the scaled threshold actually applied: deletionThreshold * capacity_pressure"
        },
        "usedBytes": {
          "type": "string",
          "format": "int64",
          "title": "the store's current used bytes"
        },
        "capacityBytes": {
          "type": "string",
          "format": "int64",
          "title": "consolidation.capacityBytes; 0 when no byte capacity is configured, in which case eviction never runs"
        },
        "candidates": {
          "type": "array",
          "items": {
            "type": "object",
            "$ref": "#/definitions/v1ForgetCandidate"
          }
        },
        "truncated": {
          "type": "boolean",
          "title": "true when more memories would be forgotten than limit returned"
        },
        "externalBytesFreed": {
          "type": "string",
          "format": "int64",
          "description": "The external axis (see Memory.external_bytes), reported on the same basis as the three fields\nabove it: what the candidates point at, what the store currently points at in total, and the\ntarget that decides whether eviction runs on that axis at all. external_bytes_freed is the sum\nover both paths, so it counts what consolidation would release as well as what eviction would.\nAll three are 0 on a deployment that stores no external sizes, which is every deployment that\nhas not asked for the feature."
        },
        "externalBytes": {
          "type": "string",
          "format": "int64",
          "title": "the store's current total external bytes; 0 when no external capacity is configured, since it is not measured then"
        },
        "capacityExternalBytes": {
          "type": "string",
          "format": "int64",
          "title": "consolidation.capacityExternalBytes; 0 when no external capacity is configured, in which case the external axis is inert"
        },
        "retainedExternalBytes": {
          "type": "string",
          "format": "int64",
          "title": "the external bytes held by consolidation.minimumRetentionInDays, the counterpart of retained_bytes"
        }
      },
      "description": "PreviewConsolidationResponse reports what a cycle would forget. The counts and byte figures are\nalways complete; candidates is a bounded sample of the individual memories, ordered by value\nascending so the least valuable - those furthest past the threshold - come first.\n\nThe preview accounts for the cycle's own ordering: consolidation runs first, so the memories it\nwould delete are excluded from the eviction pool and their bytes are already reclaimed before\neviction is considered at all. A memory therefore appears at most once, under the rule that\nwould actually claim it."
    },
    "v1QueuedCallback": {
      "type": "object",
      "properties": {
        "seq": {
          "type": "string",
          "format": "int64",
          "title": "surrogate key; also the pagination cursor"
        },
        "kind": {
          "$ref": "#/definitions/v1CallbackKind"
        },
        "cause": {
          "$ref": "#/definitions/v1DeleteCause"
        },
        "cycleId": {
          "type": "string",
          "format": "int64",
          "title": "groups every delivery one sleep cycle produced; 0 outside a cycle"
        },
        "chunk": {
          "type": "integer",
          "format": "int32",
          "title": "1-based, for a sleep-cycle delivery split across several"
        },
        "chunks": {
          "type": "integer",
          "format": "int32"
        },
        "itemCount": {
          "type": "integer",
          "format": "int32",
          "title": "how many memories or events this delivery describes"
        },
        "queuedAt": {
          "type": "string",
          "format": "int64",
          "title": "UnixNano; when the deletion happened, not when it was last attempted"
        },
        "attempts": {
          "type": "integer",
          "format": "int32"
        },
        "nextAttemptAt": {
          "type": "string",
          "format": "int64",
          "title": "UnixNano; when the dispatcher may next try"
        }
      },
      "description": "QueuedCallback is one delivery waiting to be sent, described without its payload.\n\nattempts and next_attempt_at are what separate a queue that is draining from one that is stuck:\na rising attempt count against a receding deadline is a receiver that is refusing, while a deep\nqueue with zero attempts is simply one the dispatcher has not reached yet."
    },
    "v1RecallMemoriesRequest": {
      "type": "object",
      "properties": {
        "ids": {
          "type": "array",
          "items": {
            "type": "string"
          }
        },
        "includeLinked": {
          "type": "boolean",
          "title": "also return the memories linked to those recalled, one hop, in either direction. The linked memories are returned as an associative recall, not counted as directly recalled: their recall_count is untouched (see consolidation.linkRecallPropagation for what a recall does to a neighbour's decay clock)"
        }
      }
    },
    "v1ReplaceMemoriesWithSummaryResponse": {
      "type": "object",
      "properties": {
        "id": {
          "type": "string"
        },
        "memoriesReplaced": {
          "type": "integer",
          "format": "int32"
        }
      }
    },
    "v1SearchMemoriesRequest": {
      "type": "object",
      "properties": {
        "query": {
          "type": "string"
        },
        "limit": {
          "type": "integer",
          "format": "int32",
          "title": "maximum results; a value \u003c= 0 selects the default (10)"
        },
        "eventId": {
          "type": "string",
          "title": "optional: restrict matches to a single event"
        },
        "reinforce": {
          "type": "boolean",
          "title": "route matches through recall, reinforcing them"
        },
        "group": {
          "type": "string",
          "title": "optional: restrict matches to memories carrying this group label"
        },
        "mode": {
          "$ref": "#/definitions/v1SearchMode",
          "title": "how to match; unset means KEYWORD"
        },
        "includeLinked": {
          "type": "boolean",
          "title": "also return the memories linked to each match, one hop, in either direction; appended after the ranked matches and never counted as matches themselves"
        },
        "metadata": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "title": "optional: restrict matches to memories whose metadata carries every one of these \"key=value\" pairs (AND, exact match), exactly as GetMemoriesRequest.metadata. Applied inside the search index rather than to the results, so it narrows the candidates that ranking sees and limit still means what it says"
        }
      },
      "description": "SearchMemoriesRequest queries the secondary content-search index for memories matching the\nquery. Results are always re-read from the primary store, which remains authoritative; ids the\nindex returns that the primary store no longer holds are silently dropped. When reinforce is\ntrue the matched memories are recalled - resetting their decay clocks and raising their\neffective significance - rather than merely fetched, and only the memories actually returned are\nreinforced."
    },
    "v1SearchMode": {
      "type": "string",
      "enum": [
        "SEARCH_MODE_UNSPECIFIED",
        "SEARCH_MODE_KEYWORD",
        "SEARCH_MODE_SEMANTIC",
        "SEARCH_MODE_HYBRID"
      ],
      "default": "SEARCH_MODE_UNSPECIFIED",
      "description": "SearchMode selects how a search decides which memories match.\n\nNot every mode is available in every deployment, and which are is a property of the deployment\nrather than of the caller: KEYWORD needs a content-search backend (built in on the sqlite driver,\nOpenSearch otherwise), while SEMANTIC and HYBRID additionally need an embedding model and\nOpenSearch's vector index. An unavailable mode is rejected with FAILED_PRECONDITION naming what\nis missing; WhoAmI reports the available modes so a client can choose without probing.\n\n - SEARCH_MODE_UNSPECIFIED: treated as KEYWORD, so an existing caller's behaviour is unchanged\n - SEARCH_MODE_KEYWORD: match on the words in the body; exact terms, error codes, and identifiers\n - SEARCH_MODE_SEMANTIC: match on meaning via embeddings; finds paraphrase the words would miss\n - SEARCH_MODE_HYBRID: both, their rankings fused; usually the best of the three"
    },
    "v1SignificanceExtremum": {
      "type": "string",
      "enum": [
        "SIGNIFICANCE_EXTREMUM_UNSPECIFIED",
        "SIGNIFICANCE_EXTREMUM_HIGHEST",
        "SIGNIFICANCE_EXTREMUM_LOWEST"
      ],
      "default": "SIGNIFICANCE_EXTREMUM_UNSPECIFIED",
      "description": "SignificanceExtremum selects \"all events tied at the highest (or lowest) significance value\"\ninstead of a min/max range - the tied set is computed dynamically from whichever events match\nGetEventsRequest's other filters (time range, group), not a fixed value the caller supplies.\nMutually exclusive with significance_min/significance_max (InvalidArgument if both are set).\n\n - SIGNIFICANCE_EXTREMUM_UNSPECIFIED: no extremum filtering; significance_min/significance_max (if set) apply as a normal range\n - SIGNIFICANCE_EXTREMUM_HIGHEST: return only events at the highest significance value among those matching the other filters\n - SIGNIFICANCE_EXTREMUM_LOWEST: return only events at the lowest significance value among those matching the other filters"
    },
    "v1SignificancePlacement": {
      "type": "object",
      "properties": {
        "mode": {
          "$ref": "#/definitions/SignificancePlacementMode",
          "title": "the placement mode"
        },
        "anchor": {
          "type": "integer",
          "format": "int32",
          "title": "anchor significance value; must be \u003e= 1 unless anchor_id is set"
        },
        "anchorId": {
          "type": "string",
          "title": "anchor by an existing item's id, whose significance is looked up; that item must already be ranked (significance \u003e 0)"
        },
        "upper": {
          "type": "integer",
          "format": "int32",
          "title": "BETWEEN only: the upper bound significance; must be \u003e= 1 (and greater than anchor) unless upper_id is set"
        },
        "upperId": {
          "type": "string",
          "title": "BETWEEN only: the upper bound by an existing item's id; that item must already be ranked (significance \u003e 0)"
        }
      },
      "description": "SignificancePlacement positions an item's significance relative to existing values instead of\ntaking the absolute `significance` field. It exists because significance is a dense integer with\nno room between adjacent values: this lets a client say \"just above the 5s\" or \"between 5 and 6\"\nand have the server open a gap for it. Write-only on StoreEvent/StoreMemory/UpdateMemory/\nUpdateEvent and UpdateEventSignificance; it is never populated on read responses. When mode is UNSPECIFIED the\nabsolute `significance` value is used as-is (0 meaning unranked)."
    },
    "v1SortDirection": {
      "type": "string",
      "enum": [
        "SORT_DIRECTION_UNSPECIFIED",
        "SORT_DIRECTION_ASC",
        "SORT_DIRECTION_DESC"
      ],
      "default": "SORT_DIRECTION_UNSPECIFIED",
      "description": "SortDirection reverses the order a listing's order_by field sorts in (GetEventsRequest.order_dir,\nGetMemoriesRequest.order_dir).\n\nUNSPECIFIED keeps each order_by value's NATURAL direction rather than meaning \"ascending\": the\nmagnitude and time fields sort descending (most significant, most recent, most recalled first -\nwhich is what a listing is nearly always asked for, and what every one of these listings did\nbefore there was a direction to set), while the lexical ones - id, group, and an event's name -\nsort ascending, alphabetical order being the only reading of those anyone wants first. The\nnatural direction of each value is stated on order_by itself.\n\n - SORT_DIRECTION_UNSPECIFIED: sort in the order_by field's natural direction (see above)\n - SORT_DIRECTION_ASC: ascending, whatever the field's natural direction\n - SORT_DIRECTION_DESC: descending, whatever the field's natural direction"
    },
    "v1StoreEventResponse": {
      "type": "object",
      "properties": {
        "id": {
          "type": "string",
          "title": "the stored event's id; empty when the event was not retained"
        },
        "memoryCount": {
          "type": "integer",
          "format": "int32",
          "title": "nested memories retained (best-effort; those rejected/dropped are not reported individually)"
        },
        "rejected": {
          "type": "boolean",
          "title": "true when the event was dropped for significance below event.minimumSignificance"
        }
      },
      "description": "StoreEventResponse carries the stored event's id and how many of its nested memories were\nretained. An event whose significance is below event.minimumSignificance is quietly forgotten,\nlike a brain that simply does not retain the insignificant: no error is returned, id is empty,\nmemory_count is 0 (its nested memories are not stored either), and rejected is true.\n\nNested-memory creation is best-effort: the event is committed first, so a nested memory that\nfails validation, is dropped for insignificance, or hits a store error does not fail the event\ncreate and is not individually reported here - memory_count is only the count actually retained.\nStore memories individually if you need per-memory success/failure."
    },
    "v1StoreMemoriesRequest": {
      "type": "object",
      "properties": {
        "memories": {
          "type": "array",
          "items": {
            "type": "object",
            "$ref": "#/definitions/v1Memory"
          },
          "title": "at most 500; a larger batch is refused with INVALID_ARGUMENT rather than truncated"
        }
      },
      "description": "StoreMemoriesRequest carries a batch of unrelated memories for the write path. Each is written\nindependently, so they need share nothing - not an event, not a group, not a significance."
    },
    "v1StoreMemoriesResponse": {
      "type": "object",
      "properties": {
        "results": {
          "type": "array",
          "items": {
            "type": "object",
            "$ref": "#/definitions/v1StoreMemoryResult"
          }
        },
        "stored": {
          "type": "integer",
          "format": "int32",
          "title": "memories written"
        },
        "rejected": {
          "type": "integer",
          "format": "int32",
          "title": "memories dropped for significance below memory.minimumSignificance"
        },
        "failed": {
          "type": "integer",
          "format": "int32",
          "title": "memories that could not be written; each carries its code and error in results"
        }
      },
      "description": "StoreMemoriesResponse reports one result per requested memory, positionally: results[i]\ndescribes the request's memories[i], and len(results) always equals len(memories)."
    },
    "v1StoreMemoryResponse": {
      "type": "object",
      "properties": {
        "id": {
          "type": "string",
          "title": "the stored memory's id; empty when the memory was not retained"
        },
        "rejected": {
          "type": "boolean",
          "title": "true when the memory was dropped for significance below memory.minimumSignificance"
        }
      },
      "description": "StoreMemoryResponse carries the stored memory's id. A memory whose significance is below\nmemory.minimumSignificance is quietly forgotten, like a brain that simply does not retain the\ninsignificant: no error is returned, id is empty, and rejected is true."
    },
    "v1StoreMemoryResult": {
      "type": "object",
      "properties": {
        "id": {
          "type": "string",
          "title": "the stored memory's id; empty when the memory was rejected or failed"
        },
        "rejected": {
          "type": "boolean",
          "title": "true when the memory was dropped for significance below memory.minimumSignificance - not an error, exactly as on StoreMemoryResponse"
        },
        "code": {
          "type": "integer",
          "format": "int32",
          "title": "the gRPC status code this memory failed with (google.rpc.Code; 0 = OK), so a producer can tell a permanent rejection from a retryable one without parsing error"
        },
        "error": {
          "type": "string",
          "title": "the failure message; empty unless code is non-zero"
        }
      },
      "description": "StoreMemoryResult is one memory's outcome. Exactly one of the three states holds: stored (id\nset), rejected for insignificance (rejected true, id empty, code OK), or failed (code non-zero\nand error naming why, id empty).\n\nA failure is reported here rather than as the call's error, so one bad record does not discard\nthe batch. A caller that checks only the RPC's error status therefore learns nothing about the\nindividual records - read the results."
    },
    "v1SummarisationCandidate": {
      "type": "object",
      "properties": {
        "eventId": {
          "type": "string"
        },
        "eventName": {
          "type": "string"
        },
        "memoryCount": {
          "type": "integer",
          "format": "int32"
        }
      },
      "description": "SummarisationCandidate identifies an event whose memories have accumulated enough, and gone\nquiet for long enough, to be worth condensing into a single summary memory via\nReplaceMemoriesWithSummary."
    },
    "v1SummariseMemoriesResponse": {
      "type": "object",
      "properties": {
        "id": {
          "type": "string",
          "title": "the new summary memory's id"
        },
        "memoriesReplaced": {
          "type": "integer",
          "format": "int32",
          "title": "how many memories the summary replaced"
        },
        "summary": {
          "type": "string",
          "title": "the LLM-generated summary text stored in the new memory"
        }
      }
    },
    "v1TopologyAttribute": {
      "type": "object",
      "properties": {
        "key": {
          "type": "string"
        },
        "value": {
          "type": "string"
        }
      },
      "description": "TopologyAttribute is one piece of a node's configuration, as a key/value pair.\n\nA bag rather than a typed message per kind, because the alternative grows a proto field for every\ncomponent type and a rendering branch to match; the keys a kind carries are documented with the\nbuilder that produces them. Values are already redacted: no credential, secret, or DSN password\never reaches this message - see GetTopology."
    },
    "v1TopologyEdge": {
      "type": "object",
      "properties": {
        "fromId": {
          "type": "string"
        },
        "toId": {
          "type": "string"
        },
        "label": {
          "type": "string",
          "description": "what crosses it: \"reads/writes\", \"indexes\", \"verifies\", ..."
        },
        "optional": {
          "type": "boolean",
          "title": "the edge exists only because an optional feature is enabled"
        }
      },
      "description": "TopologyEdge is a relationship between two nodes, directed from the component that initiates the\nconnection to the one it reaches - which is the direction that matters operationally, since it is\nwhat a firewall rule, a credential, and an outage all follow."
    },
    "v1TopologyNode": {
      "type": "object",
      "properties": {
        "id": {
          "type": "string",
          "description": "stable within a response: \"self\", \"store\", \"declared:\u003cname\u003e\", ..."
        },
        "kind": {
          "$ref": "#/definitions/v1TopologyNodeKind"
        },
        "name": {
          "type": "string",
          "title": "what to label it; for a declared component, the name the operator gave"
        },
        "detail": {
          "type": "string",
          "title": "the redacted endpoint, host, or file path; empty where there is none"
        },
        "source": {
          "$ref": "#/definitions/v1TopologyNodeSource"
        },
        "status": {
          "$ref": "#/definitions/v1TopologyStatus"
        },
        "statusDetail": {
          "type": "string",
          "description": "status_detail explains a status that is not OK, in a form meant to be shown to an operator\n(\"connection refused\", \"index red\", \"readiness reports store unreachable\"). Empty when OK."
        },
        "checkedAt": {
          "type": "string",
          "format": "int64",
          "description": "checked_at is when the probe behind status last ran (UnixNano), or 0 when the node is never\nprobed. It is reported rather than implied because probing is a background ticker, not\nsomething the RPC does: a caller is always reading a snapshot, and how old it is matters when\nsomething has just broken."
        },
        "version": {
          "type": "string",
          "title": "where the component reports one; empty otherwise"
        },
        "attributes": {
          "type": "array",
          "items": {
            "type": "object",
            "$ref": "#/definitions/v1TopologyAttribute"
          }
        }
      },
      "description": "TopologyNode is one component of the deployment."
    },
    "v1TopologyNodeKind": {
      "type": "string",
      "enum": [
        "TOPOLOGY_NODE_KIND_UNSPECIFIED",
        "TOPOLOGY_NODE_KIND_INSTANCE",
        "TOPOLOGY_NODE_KIND_STORE",
        "TOPOLOGY_NODE_KIND_SEARCH_INDEX",
        "TOPOLOGY_NODE_KIND_SUMMARISER",
        "TOPOLOGY_NODE_KIND_EMBEDDER",
        "TOPOLOGY_NODE_KIND_OBJECT_STORE",
        "TOPOLOGY_NODE_KIND_TRANSFER_TARGET",
        "TOPOLOGY_NODE_KIND_IDENTITY_PROVIDER",
        "TOPOLOGY_NODE_KIND_COLLECTOR",
        "TOPOLOGY_NODE_KIND_BRIDGE",
        "TOPOLOGY_NODE_KIND_INGESTOR",
        "TOPOLOGY_NODE_KIND_MCP_BRIDGE",
        "TOPOLOGY_NODE_KIND_CLIENT",
        "TOPOLOGY_NODE_KIND_CALLBACK_RECEIVER"
      ],
      "default": "TOPOLOGY_NODE_KIND_UNSPECIFIED",
      "description": "TopologyNodeKind names what a component in the deployment graph IS. It is deliberately coarse:\nthe console draws one shape per kind and an operator reads one word, so a kind exists only where\nit changes how a node is drawn or what its attributes mean.\n\nThe set describes the deployment's component types, not what a given build happens to emit. An\ninstance can only report what it knows (see TopologyNodeSource), so the kinds that dial IN -\nbridges, the ingestor, MCP servers, clients - appear only where an operator has declared them or\nwhere they have been observed calling.\n\n - TOPOLOGY_NODE_KIND_UNSPECIFIED: not set\n - TOPOLOGY_NODE_KIND_INSTANCE: a Hippocampus instance: this one, or a peer sharing its store\n - TOPOLOGY_NODE_KIND_STORE: the primary store (SQLite file, PostgreSQL, MySQL)\n - TOPOLOGY_NODE_KIND_SEARCH_INDEX: the secondary content-search index\n - TOPOLOGY_NODE_KIND_SUMMARISER: the embedded LLM behind SummariseMemories\n - TOPOLOGY_NODE_KIND_EMBEDDER: the embedding model behind semantic search\n - TOPOLOGY_NODE_KIND_OBJECT_STORE: the S3 bucket behind Export/Import\n - TOPOLOGY_NODE_KIND_TRANSFER_TARGET: the instance Transfer sends to\n - TOPOLOGY_NODE_KIND_IDENTITY_PROVIDER: the IdP whose JWKS verifies tokens\n - TOPOLOGY_NODE_KIND_COLLECTOR: the OTLP endpoint metrics and traces are exported to\n - TOPOLOGY_NODE_KIND_BRIDGE: an event-sourcing broker bridge\n - TOPOLOGY_NODE_KIND_INGESTOR: the edge-to-central ingestor\n - TOPOLOGY_NODE_KIND_MCP_BRIDGE: an MCP server bridging an LLM host to this service\n - TOPOLOGY_NODE_KIND_CLIENT: any other client of this service\n - TOPOLOGY_NODE_KIND_CALLBACK_RECEIVER: the endpoint outbound callbacks are delivered to"
    },
    "v1TopologyNodeSource": {
      "type": "string",
      "enum": [
        "TOPOLOGY_NODE_SOURCE_UNSPECIFIED",
        "TOPOLOGY_NODE_SOURCE_SELF",
        "TOPOLOGY_NODE_SOURCE_CONFIGURED",
        "TOPOLOGY_NODE_SOURCE_DISCOVERED",
        "TOPOLOGY_NODE_SOURCE_DECLARED",
        "TOPOLOGY_NODE_SOURCE_OBSERVED"
      ],
      "default": "TOPOLOGY_NODE_SOURCE_UNSPECIFIED",
      "description": "TopologyNodeSource says HOW this instance came to know about a node, and is the most important\nfield in the message. An instance is not a control plane: it knows itself and whatever it dials\noutward, and everything else in a deployment - replicas sharing its database, bridges, the\ningestor, MCP servers, the CLI - dials IN, holding an address for the service rather than the\nservice holding one for them.\n\nRather than present a picture that pretends otherwise, every node says where it came from, so a\nclient can render \"what this instance knows\" instead of implying ground truth. A deployment whose\ndiagram looks sparse is usually one that has declared nothing, not one that is running nothing.\n\n - TOPOLOGY_NODE_SOURCE_UNSPECIFIED: not set\n - TOPOLOGY_NODE_SOURCE_SELF: this process\n - TOPOLOGY_NODE_SOURCE_CONFIGURED: something this instance dials, read from its own configuration\n - TOPOLOGY_NODE_SOURCE_DISCOVERED: a peer instance found in the shared store\n - TOPOLOGY_NODE_SOURCE_DECLARED: an operator listed it under topology.components\n - TOPOLOGY_NODE_SOURCE_OBSERVED: it has called this instance with a verified token"
    },
    "v1TopologyStatus": {
      "type": "string",
      "enum": [
        "TOPOLOGY_STATUS_UNSPECIFIED",
        "TOPOLOGY_STATUS_OK",
        "TOPOLOGY_STATUS_DEGRADED",
        "TOPOLOGY_STATUS_UNREACHABLE",
        "TOPOLOGY_STATUS_DISABLED"
      ],
      "default": "TOPOLOGY_STATUS_UNSPECIFIED",
      "description": "TopologyStatus is a node's last known health.\n\nUNSPECIFIED is both the wire default and a meaning: not probed. Several nodes are deliberately\nnever probed - the OTLP collector, because export is fire-and-forget and probing it would mean a\nsecond dial for no operational gain, and the identity provider, because a console poll must not\nbecome load on somebody's IdP. Those report UNSPECIFIED with a checked_at of 0, which is honest,\nwhere a green tick would not be.\n\n - TOPOLOGY_STATUS_UNSPECIFIED: not probed; checked_at is 0\n - TOPOLOGY_STATUS_OK: reachable and serving\n - TOPOLOGY_STATUS_DEGRADED: reachable, but reporting a problem of its own; see status_detail\n - TOPOLOGY_STATUS_UNREACHABLE: the probe failed; see status_detail\n - TOPOLOGY_STATUS_DISABLED: configured off, so nothing to probe"
    },
    "v1TransferRequest": {
      "type": "object",
      "properties": {
        "clear": {
          "type": "boolean"
        }
      },
      "description": "Transfer streams every event and memory directly to a centralised instance's ImportBatch RPC\n(address from transfer.targetAddress); like Export it records a manifest, and with clear set\ndeletes the captured records once the target has accepted them all."
    },
    "v1TransferResponse": {
      "type": "object",
      "properties": {
        "manifestId": {
          "type": "string"
        },
        "eventsTransferred": {
          "type": "integer",
          "format": "int32"
        },
        "memoriesTransferred": {
          "type": "integer",
          "format": "int32"
        },
        "memoriesCleared": {
          "type": "integer",
          "format": "int32"
        },
        "eventsCleared": {
          "type": "integer",
          "format": "int32"
        }
      }
    },
    "v1WhoAmIResponse": {
      "type": "object",
      "properties": {
        "clientId": {
          "type": "string"
        },
        "role": {
          "type": "string"
        },
        "authEnabled": {
          "type": "boolean"
        },
        "searchModes": {
          "type": "array",
          "items": {
            "$ref": "#/definitions/v1SearchMode"
          },
          "description": "search_modes lists the SearchMemories modes this deployment can serve, so a client can adapt\nrather than discover an unavailable mode by having a search rejected. It depends on the\nstorage driver and on whether OpenSearch and an embedding model are configured - never on the\ncaller - so it is the same for every caller of a given instance. An empty list means content\nsearch is unavailable entirely and SearchMemories is rejected in every mode."
        },
        "groups": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "description": "groups is the caller's group scope: the group labels whose records this token may read and\nwrite. Empty means unscoped - the whole store - which is what every token is unless the\ndeployment issues scoped ones, so a client must read group_scoped rather than infer from the\nlist being empty."
        },
        "groupScoped": {
          "type": "boolean",
          "description": "group_scoped reports whether a scope is in force for this caller. It exists because an empty\ngroups list is ambiguous on its own and the two readings are opposites: unscoped (see\neverything) versus scoped to nothing. A client showing \"you can see N groups\" must branch on\nthis, not on len(groups).\n\nWhen true, writes are stamped with the caller's group, Purge/Sleep/PreviewConsolidation are\nrefused, and records outside the scope report NotFound - so a console can explain an empty\nstore rather than leave it looking like data loss."
        },
        "summariserEnabled": {
          "type": "boolean",
          "description": "summariser_enabled reports whether this deployment has the embedded LLM configured\n(llm.enabled), i.e. whether SummariseMemories can serve. Reported for the same reason as\nsearch_modes and with the same properties: it is a property of the deployment rather than the\ncaller, so a client can offer service-authored summarisation only where it exists instead of\ndiscovering its absence through a FAILED_PRECONDITION. It says nothing about\nReplaceMemoriesWithSummary, which needs no summariser - the caller writes that summary."
        },
        "consolidationEnabled": {
          "type": "boolean",
          "description": "consolidation_enabled reports whether this instance runs the sleep cycle\n(consolidation.enabled). It is the deployment property behind a whole family of refusals:\nSleep, PreviewConsolidation and ExplainConsolidation are all FAILED_PRECONDITION on a replica,\nbecause its store is consolidated by whichever instance holds the single-consolidator lock and\nunder that instance's configuration rather than this one's. A client that would otherwise\noffer decay figures, a dry run, or a forced cycle can hide all three instead of presenting\ncontrols certain to be refused - the same argument as search_modes and summariser_enabled."
        },
        "tombstonesEnabled": {
          "type": "boolean",
          "description": "tombstones_enabled reports whether the forgotten log is being recorded\n(consolidation.tombstones.enabled). Unlike the flags above this one gates a reader rather than\na writer: GetForgottenMemories does not refuse when the log is off, it returns an empty page\nwith enabled false. That distinction is exactly why it belongs here - \"nothing was written\ndown\" and \"nothing has been forgotten\" render identically until a client is told which it is\nlooking at, and only the first is worth hiding the control for."
        },
        "topologyTier": {
          "type": "string",
          "description": "topology_tier is the minimum authorization tier GetTopology requires on this deployment\n(topology.minimumTier), as a tier name. It is reported rather than resolved to a bool because\nit belongs with its neighbours above, which are all properties of the DEPLOYMENT and never of\nthe caller; a client compares it against the role it was told in this same response, which is\nwhere it already decides what to offer.\n\nEmpty means the RPC is unavailable here (topology.enabled false), which is not the same as\nbeing refused it - so a client can leave the view out entirely rather than offer a control\ncertain to fail."
        },
        "version": {
          "type": "string",
          "description": "version is the build this instance is running, in the form --version prints and /healthz\nreturns. It is here because those two are the only other places it appears, and neither is\nreachable by a gRPC client: the CLI, the MCP bridge, the broker bridges and the ingestor could\npreviously learn it only from GetTopology's self node, which is optional (topology.enabled),\ntier-configurable, and refused outright to a group-scoped caller - so a client that wanted to\ncheck what it was talking to had three ways to be told nothing.\n\nEmpty when the build carries no identification at all, which is what a binary built without\nmodule information reports."
        },
        "callbacksEnabled": {
          "type": "boolean",
          "description": "callbacks_enabled reports whether outbound callbacks are configured here\n(callbacks.url, and this instance running the dispatcher). It is a deployment property like\nits neighbours above, and it is here for tombstones_enabled's argument exactly: an empty\nqueue and a disabled feature render identically, so a client shown GetCallbackQueue's empty\npage cannot tell \"nothing is waiting to be delivered\" from \"nothing will ever be delivered\"."
        }
      },
      "description": "WhoAmIResponse reports the caller's identity to a client so it can tailor its UI. role is the\ncaller's effective authorization tier (\"reader\", \"writer\", or \"admin\"); auth_enabled is false\nwhen the service runs without authentication, in which case role is \"admin\" (unrestricted) and\nclient_id is empty."
    }
  },
  "securityDefinitions": {
    "BearerAuth": {
      "type": "apiKey",
      "description": "A signed JWT bearer token. Enter it as: Bearer \u003ctoken\u003e",
      "name": "Authorization",
      "in": "header"
    }
  },
  "security": [
    {
      "BearerAuth": []
    }
  ]
}
